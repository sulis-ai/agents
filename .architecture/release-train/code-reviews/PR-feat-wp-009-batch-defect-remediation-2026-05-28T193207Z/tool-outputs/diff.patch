diff --git a/.github/workflows/release-on-merge.yml b/.github/workflows/release-on-merge.yml
index 1feeef2..0f01fb7 100644
--- a/.github/workflows/release-on-merge.yml
+++ b/.github/workflows/release-on-merge.yml
@@ -28,12 +28,16 @@ jobs:
   release:
     # Loop-guard: skip the bot's own release commits (prefix matches the commit
     # message assembled in the "Commit, tag, push" step below, exactly).
-    # The value is single-quoted at the YAML level because the `: ` inside the
+    # The whole value is a single-quoted YAML scalar because the `: ` inside the
     # string literal would otherwise be read by YAML as a nested mapping. The
-    # expression's own string literal uses double quotes so the wrapping is
-    # unambiguous. The prefix "release: sulis" matches the commit message in the
-    # final "Commit, tag, push" step exactly — keep the two in lockstep.
-    if: '${{ !startsWith(github.event.head_commit.message, "release: sulis") }}'
+    # expression's own string literal is SINGLE-quoted too — GitHub Actions
+    # expressions accept single-quoted literals only (a double-quoted literal is
+    # rejected at expression evaluation while still passing YAML lint). Inside a
+    # single-quoted YAML scalar each inner `'` is doubled, so ''release: sulis''
+    # renders as the expression literal 'release: sulis'. The prefix
+    # 'release: sulis' matches the commit message in the final "Commit, tag,
+    # push" step exactly — keep the two in lockstep.
+    if: '${{ !startsWith(github.event.head_commit.message, ''release: sulis'') }}'
     runs-on: ubuntu-latest
     timeout-minutes: 15
     permissions:
diff --git a/plugins/sulis/scripts/_changeset.py b/plugins/sulis/scripts/_changeset.py
index 2998a86..5ed5f84 100644
--- a/plugins/sulis/scripts/_changeset.py
+++ b/plugins/sulis/scripts/_changeset.py
@@ -187,7 +187,7 @@ def changeset_filename(primitive: str, slug: str, created_at: datetime) -> str:
 
 
 def write_changeset(
-    changesets_dir: Path,
+    changesets_dir: str | Path,
     *,
     change_id: str,
     primitive: str,
@@ -199,12 +199,16 @@ def write_changeset(
 ) -> Path:
     """Write one changeset YAML; return the path written.
 
+    `changesets_dir` accepts a `str` OR a `pathlib.Path` — a `str` is coerced
+    to `Path` at entry (the ship flow passes the plain string `'.changesets'`),
+    so a string argument no longer raises `AttributeError` on `.mkdir()`.
     Creates `changesets_dir` if absent. The filename is the collision-proof
     triple key (`changeset_filename`). `created_at` defaults to now (UTC).
     `slug` is the human filename component — the change's own slug (e.g.
     `release-train`); when omitted it falls back to the `change_id` so the
     helper is self-contained for direct callers. WP-002 passes the human slug.
     """
+    changesets_dir = Path(changesets_dir)
     when = created_at or datetime.now(timezone.utc)
     changesets_dir.mkdir(parents=True, exist_ok=True)
     name = changeset_filename(primitive, slug=slug or change_id, created_at=when)
@@ -223,13 +227,17 @@ def write_changeset(
     return path
 
 
-def read_changesets(changesets_dir: Path) -> list[dict]:
+def read_changesets(changesets_dir: str | Path) -> list[dict]:
     """Read every `*.yaml` in the dir into dicts.
 
+    `changesets_dir` accepts a `str` OR a `pathlib.Path` — a `str` is coerced
+    to `Path` at entry (symmetric with `write_changeset`), so a string argument
+    no longer raises `AttributeError` on `.is_dir()`.
     Ignores non-`.yaml` files (README.md, scratch .txt). A missing dir → [].
     Order is sorted by filename for determinism (the triple-key filename sorts
     primitive-then-slug-then-time).
     """
+    changesets_dir = Path(changesets_dir)
     if not changesets_dir.is_dir():
         return []
     records = []
diff --git a/plugins/sulis/scripts/tests/unit/test_changeset.py b/plugins/sulis/scripts/tests/unit/test_changeset.py
index ff57bb5..1e57fe0 100644
--- a/plugins/sulis/scripts/tests/unit/test_changeset.py
+++ b/plugins/sulis/scripts/tests/unit/test_changeset.py
@@ -222,6 +222,39 @@ def test_write_read_changeset_round_trip(tmp_path):
     assert fix["summary"].strip() == "Fix the journal step guard."
 
 
+def test_write_read_changeset_round_trip_accepts_str_dir(tmp_path):
+    """A plain `str` dir round-trips through write + read (WP-009 / CR-BATCH-01).
+
+    The ship flow's step 4.7 passes the plain string `'.changesets'` (not a
+    `Path`). Pre-fix, `write_changeset` called `.mkdir()` on the `str` and
+    `read_changesets` called `.is_dir()` on it, both raising `AttributeError`,
+    so the producer crashed and `dev` accumulated no changesets. The keystone
+    now coerces `Path(changesets_dir)` at entry of both functions; this test
+    pins the `str` path so the regression cannot return. Mirrors
+    `test_write_read_changeset_round_trip`; the only difference is the dir
+    argument is a `str`, not a `Path`.
+    """
+    str_dir = str(tmp_path / ".changesets")
+    t = datetime(2026, 5, 28, 17, 30, 0, tzinfo=timezone.utc)
+    path = cs.write_changeset(
+        str_dir, change_id=_CHANGE_ID, primitive="fix", tier="patch",
+        touches_plugin=True, summary="Round-trip a str dir.",
+        slug="release-train", created_at=t,
+    )
+    assert path.exists()
+
+    # Read back from the SAME str dir — exercises read_changesets' coercion too.
+    records = cs.read_changesets(str_dir)
+    assert len(records) == 1
+    record = records[0]
+    assert record["change_id"] == _CHANGE_ID
+    assert record["primitive"] == "fix"
+    assert record["tier"] == "patch"
+    assert record["touches_plugin"] is True
+    assert record["summary"].strip() == "Round-trip a str dir."
+    assert record["created_at"] == "2026-05-28T17:30:00Z"
+
+
 def test_write_changeset_honours_human_slug(tmp_path):
     """WP-002 passes the change's human slug → it appears in the filename
     (the ADR-005 worked-example shape, not the raw change_id)."""
