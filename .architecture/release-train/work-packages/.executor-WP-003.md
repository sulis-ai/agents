# Executor journal — WP-003

> Started: 2026-05-28T18:39:59Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| python3 | present | — |
| jq | present | — |
| pyyaml (yaml.safe_load) | present | — |
| actionlint | absent | validate YAML via python3 yaml.safe_load; structural review against proven release-prod.yml shape |
| gh | present | — |

## Plan

**Approach.** Create primitive: author a single net-new GitHub Actions workflow .github/workflows/release-on-merge.yml as the one bump authority (ADR-004). Key decision: take ADR-004 alt-2 / EP-03 single-source — call _changeset.py directly (cumulative_tier, next_version, read_changesets) from the checked-out repo via sys.path rather than re-implementing the YAML reader + tier math in bash; verified import-clean against WP-001 vectors in pre-flight. Key risk: the three-value-one-tier bump (ADR-003) + tag from metadata.version — the post-bump verification step is the safety net.

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | Author post-bump verification step (the infra 'Red' assertion) in .github/workflows/release-on-merge.yml — re-reads all three values, fails if any did not move | 2 (RED) | done | infra: assertion-first per WP DoD; no unit tests (bash-in-YAML) |
| 2 | Hand-trace tier math vs WP-001 test vectors (0.77.0 minor->0.78.0, 1.122.0 minor->1.123.0, 0.77.0 major->1.0.0) and assert the workflow compute step produces identical results via _changeset.py | 2 (RED) | done | — |
| 3 | Author all 9 steps of .github/workflows/release-on-merge.yml (trigger push:main, permissions contents:write, job-level loop-guard if:, checkout fetch-depth:0, detect changesets, VERSION_DRIFT guard, compute tier+version, assemble CHANGELOG, three-value bump, post-bump verify, git rm changesets, commit/tag/push) | 3 (GREEN) | done | — |
| 4 | Validate the workflow YAML parses (python3 yaml.safe_load) and matches the proven release-prod.yml grammar shape | 3 (GREEN) | done | — |
| 5 | Blue: call _changeset.py directly for cumulative_tier + next_version + read_changesets (ADR-004 alt 2 / EP-03 single-source) instead of re-implemented bash YAML reader; confirm import-clean from checkout | 4 (BLUE) | done | Single-source achieved: workflow calls _changeset.cumulative_tier/next_version/read_changesets directly via sys.path; no bash YAML reader, no tier-math duplication. re-implementer rules moot. |
| 6 | Blue: confirm CHANGELOG header shape compatible with release-prod.yml awk reader (reads first ## section); confirm loop-guard prefix matches step-9 commit message exactly | 4 (BLUE) | done | CHANGELOG header '## v<plugin> — <date>' verified awk-reader compatible (sandbox extracted top section); loop-guard prefix 'release: sulis' is exact prefix of step-9 commit message — lockstep confirmed. |
| 7 | Docs: no-op check — WP has no docs_to_update; the .changesets/README.md contract is owned by WP-001/008; the workflow is self-documenting via comments | 5 (DOCS) | skipped | No-op: WP has no docs_to_update; the workflow is self-documenting via header comments; .changesets/README.md contract is owned by WP-001/008; documented-flow retirement is explicitly WP-007's scope (ADR-004). |
| 8 | Lint: validate workflow YAML syntax (yaml.safe_load) — no actionlint available, fallback structural review | 6 (LINT) | done | — |
| 9 | Run /code-review against the local diff vs origin/change/create-release-train | 6.5 (REVIEW) | done | addressed: 2 inline fixes (F1 git add -A -> explicit named staging; F2 dropped unused outputs), re-verified (YAML valid, ruff clean, sandbox confirms exact staging); F3 tag-idempotency on Watch List (design choice, non-blocking); F4 nullglob benign no-action. Verdict: Approve with fixes -> all findings addressed. |
| 10 | Commit (Conventional Commits) + push feat/wp-003-release-on-merge-gha | 7 (COMMIT) | done | — |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 2 | 2026-05-28T18:41:03Z | 2026-05-28T18:41:35Z | RED trace passes: post-bump verification rejects a half-bump (the would-fail assertion); tier math single-sourced from _changeset.py matches WP-001 vectors (0.77.0 minor->0.78.0, 1.122.0 minor->1.123.0, 0.77.0 major->1.0.0). Verification step itself authored with the workflow in Step 3. |
| 3 | 2026-05-28T18:41:39Z | 2026-05-28T18:45:13Z | Authored .github/workflows/release-on-merge.yml (9 steps). YAML parses (yaml.safe_load). Sandbox smoke under bash exercised steps 4-7 end-to-end: compute 0.77.0->0.78.0 + 1.122.0->1.123.0 (tier minor); CHANGELOG '## v0.78.0' prepended; three values bumped, investor-coach untouched; post-bump verify passes + rejects half-bump; release-prod.yml awk reader extracts the new top section. Edge cases verified: no-changesets exits 0 (skip=true); VERSION_DRIFT aborts with exit 1. |
| 4 | 2026-05-28T18:45:18Z | 2026-05-28T18:45:49Z | Blue: ADR-004 alt-2 / EP-03 single-source implemented — workflow calls _changeset.py directly (cumulative_tier/next_version/read_changesets), zero bash YAML-reader or tier-math duplication (re-implementer grammar moot since no re-implementer exists). Shared primitive (read_changesets, used by both compute + CHANGELOG steps) already lives in the keystone — nothing to extract. Confirmed loop-guard prefix is exact prefix of the step-9 commit message; CHANGELOG header awk-reader compatible. |
| 5 | 2026-05-28T18:45:55Z | 2026-05-28T18:46:04Z | No-op: no docs_to_update in frontmatter; workflow self-documents via header comments; the .changesets/README.md contract is owned by WP-001/008 (out of scope); the documented-flow / manual-bump retirement is explicitly WP-007's job per ADR-004. |
| 6 | 2026-05-28T18:46:10Z | 2026-05-28T18:46:35Z | Lint PASS: YAML valid (yaml.safe_load); no tabs; no trailing whitespace; structural assertions hold (9 steps, push:main, contents:write, loop-guard if, checkout fetch-depth 0). Embedded Python snippets: py_compile OK + ruff check clean. actionlint absent (fallback used per pre-flight). |
| 6.5 | 2026-05-28T18:46:46Z | 2026-05-28T18:50:21Z | addressed: /code-review ran (bundle at .architecture/release-train/code-reviews/PR-feat-wp-003-release-on-merge-gha-2026-05-28T184751Z/ with REVIEW.md + signals.json). Verdict 'Approve with fixes' — 0 critical/high, 2 medium + 2 low. 2 inline fixes applied + re-verified (F1: git add -A -> explicit named staging; F2: dropped 2 unused step outputs). F3 (tag re-creation idempotency on partial-failure re-run) -> Watch List, non-blocking design choice. F4 (nullglob) -> benign, no action. Zero unresolved findings. |
| 7 | 2026-05-28T18:50:26Z | 2026-05-28T18:51:00Z | Pushed to feat/wp-003-release-on-merge-gha at SHA d7d93c8bc8a6133d84e5bb30746ccdae97b1333d (remote tip confirmed match). Conventional Commits: 'feat(release-train): release-on-merge.yml — the one bump authority (WP-003)'. Calling session can proceed with Step 8+ (CI poll, rebase if needed, squash-merge, security review, INDEX flip). |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

