# Executor journal — WP-013

> Started: 2026-06-09T20:52:48Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| node/npm | present | — |
| playwright | present | — |

## Plan

**Approach.** REINFORCE-Harden: prove the lane scroll perf budget rather than speculatively build machinery. A Playwright E2E seeds 200 changes into one recon lane (via /api/changes route interception, the empty-state spec pattern inverted), asserts the header count reads the true total (200) and the lane's internal scroll holds all 200, and measures no long-frame > 50ms during a programmatic scroll (longtask PerformanceObserver). Virtualisation is added ONLY if the plain-scroll budget is breached (Q-6, boring-code: don't add machinery the budget doesn't demand). Scope is the StageColumn lane render only. Key risk: long-task flakiness, mitigated by a controlled programmatic scroll measured against the 50ms budget; characterisation StageColumn.test.tsx must stay green.

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | Write apps/cockpit/e2e/board-scale.spec.ts: seed 200 changes in one recon lane via /api/changes route interception; assert header count reads 200 and the lane's internal scroll holds all 200 (first + last card reachable by scrolling); assert no long-frame > 50 ms during a programmatic scroll of the lane (S-15 / NFR-PERF-2) | 2 (RED) | done | — |
| 2 | Add a network assertion: scrolling the lane adds NO per-card /api request (no N+1 on scroll, NFR-POLL-1) | 2 (RED) | done | — |
| 3 | Make the 200-card lane meet the 60fps/<50ms budget on plain internal scroll in StageColumn lane rendering; record the measured result; virtualise ONLY if the budget is breached | 3 (GREEN) | done | — |
| 4 | Confirm the header count reflects the true total (changes.length, not a windowed subset) | 3 (GREEN) | done | — |
| 5 | If virtualisation was needed it sits behind the existing lane API (cards unchanged, EP-03); if not, no virtualisation machinery added — record the measured plain-scroll result | 4 (BLUE) | done | Budget WAS breached (6046ms long-task on plain 200-card scroll) → virtualisation built behind the existing lane API; ChangeCard unchanged (EP-03); header count stays truthful (changes.length). |
| 6 | Confirm characterisation test StageColumn.test.tsx still green | 4 (BLUE) | done | StageColumn.test.tsx characterisation: 18/18 green after the refactor. |
| 7 | Update StageColumn source-comment doc to note the proven 200-card scroll budget + the conditional-virtualisation decision | 5 (DOCS) | done | Doc surface is the StageColumn source-comment (records the proven 200-card budget, the measured 6046ms breach, and the conditional-virtualisation decision). No README/founder-doc section applies — this is a client-render concern, not an endpoint. WP-file acceptance evidence is the calling session's Step 12. |
| 8 | Run lint + typecheck + format (eslint, tsc) | 6 (LINT) | done | Fixed two noUncheckedIndexedAccess type errors in StageColumn (guarded changes[index] in getItemKey + the row map). typecheck + eslint clean; StageColumn 18/18 still green. |
| 9 | Run /code-review against the local diff and address every finding | 6.5 (REVIEW) | done | /code-review run; bundle at .architecture/cockpit-board-refresh/code-reviews/PR-feat-wp-013-lane-scale-scroll-2026-06-09T211048Z/ (REVIEW.md + signals.json). Verdict PASS, 0 findings. 1 Watch List awareness note (virtualisation+keyboard reachability), not test-grounded so no delta. |
| 10 | Commit + push feat/wp-013-lane-scale-scroll | 7 (COMMIT) | done | Committed 4cb95134 (Conventional Commits, feat scope); pushed to origin/feat/wp-013-lane-scale-scroll. Origin stamp was a no-op (run-ulid + hook absent) — unstamped by design, non-fatal. |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 2 | 2026-06-09T20:53:45Z | 2026-06-09T20:56:15Z | RED: e2e/board-scale.spec.ts written (3 tests). Count-truthful + no-N+1 pass; the no-long-frame>50ms test FAILS for the right reason — plain-scroll 200-card lane janks with a 6046ms long-task. Budget breached → virtualisation now required per Contract. |
| 3 | 2026-06-09T20:56:22Z | 2026-06-09T21:06:12Z | GREEN: virtualised the lane card list behind the existing StageColumn API with @tanstack/react-virtual (same maintainer family as react-query already in tree). Budget breach gone — all 3 board-scale PW tests pass (no long-frame>50ms on 200-card scroll; header count truthful at 200; last/first reachable; no N+1 on scroll). Full cockpit vitest suite green (1388 tests). A browser-correct observeElementRect override falls back to a fixed lane height only where there is no layout (jsdom/SSR) so unit + a11y tests render the window. |
| 4 | 2026-06-09T21:06:20Z | 2026-06-09T21:06:57Z | BLUE: virtualisation sits behind the existing lane API (cards unchanged, EP-03); LaneCardList extracted from StageColumn; observeLaneRect is a single-use, browser-correct helper; constants documented. No duplication to extract (single list; empty branch reuses .laneList). Characterisation 18/18 green; full suite 1388 green. |
| 5 | 2026-06-09T21:07:05Z | 2026-06-09T21:07:56Z | DOCS: behaviour change documented in-code (StageColumn header records the proven 200-card budget, the measured 6046ms breach, and the Q-6 conditional-virtualisation decision). No README/founder-doc section applies (client-render concern, not an endpoint). |
| 6 | 2026-06-09T21:08:01Z | 2026-06-09T21:09:16Z | LINT: tsc (server+client) clean and eslint clean after fixing two noUncheckedIndexedAccess errors in the virtualised list (guarded changes[index]). StageColumn 18/18 green. |
| 6.5 | 2026-06-09T21:09:22Z | 2026-06-09T21:12:18Z | addressed: /code-review run, bundle written (REVIEW.md + signals.json) at .architecture/cockpit-board-refresh/code-reviews/PR-feat-wp-013-lane-scale-scroll-2026-06-09T211048Z/. Verdict PASS, 0 findings (0 inline + 0 remediation + 0 exceptions). 1 non-blocking Watch List awareness note. |
| 7 | 2026-06-09T21:12:25Z | 2026-06-09T21:13:47Z | Pushed to feat/wp-013-lane-scale-scroll at SHA 4cb95134c80b2ab9b74eba89d084b10ecc44b20f. Branch tracks origin; push accepted. CI triggered. Calling session can proceed with Step 8+. |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

