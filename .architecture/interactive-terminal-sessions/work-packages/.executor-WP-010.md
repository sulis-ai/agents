# Executor journal — WP-010

> Started: 2026-06-07T15:34:29Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| node+npm | present | — |
| uv+pytest | present | — |
| playwright-chromium | present | — |
| ws (websocket) | absent | add ws + @types/ws as cockpit devDependency for the e2e terminal proxy (Step 3) |

## Plan

**Approach.** REINFORCE-Test, the final integration WP: prove the founder's five-step acceptance journey end-to-end through the REAL interface (Playwright clicking the cockpit) against a REAL AF_UNIX socket + real pty-backed fake child (MEA-09 no mocks), and aggregate the headless-chat regression gate. The one piece of genuine wiring: replace WP-008's documented notYetWiredTransport with a live browser WS SocketTransport, bridged to the Python SocketServer by an e2e-harness WS->AF_UNIX proxy (kept OUT of the read-only Express app so the read-only-inventory gate stays green). Key risk: the read-only server constraint + browser-cannot-open-AF_UNIX means the live transport must route through a WS proxy in the harness, not the GET-only app.

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | RED: Playwright e2e apps/cockpit/e2e/live-terminal.spec.ts — acceptance #1 open terminal tab → see scrollback snapshot (not blank), against dev cockpit + real AF_UNIX socket + pty fake child | 2 (RED) | done | — |
| 2 | RED: live-terminal.spec.ts — acceptance #2 type a command → keystrokes reach live session, output appears (feed round-trip) | 2 (RED) | done | — |
| 3 | RED: live-terminal.spec.ts — acceptance #3 close tab + reopen → session still alive, scrollback catches up (detach/reattach) | 2 (RED) | done | — |
| 4 | RED: live-terminal.spec.ts — Playwright-axe clean on the live terminal surface (WPF-10) | 2 (RED) | done | — |
| 5 | RED: plugins/sulis/scripts/tests/integration/test_terminal_lifecycle.py::test_restart_recreates_pty — pty child dies → restart re-creates pty, viewer reattaches (#5) | 2 (RED) | done | — |
| 6 | RED: test_terminal_lifecycle.py::test_idle_pty_evicted — idle pty with 0 viewers evicted by same timer (#5) | 2 (RED) | done | — |
| 7 | RED: test_terminal_lifecycle.py regression gate #4 — base contract suite green with io_mode defaulted + attach_on_pipe → NOT_PTY_SESSION aggregation | 2 (RED) | done | — |
| 8 | GREEN: client browser WS SocketTransport (apps/cockpit/client/src/terminal/socketWsTransport.ts) — the live-socket seam createTerminalBridge consumes (WP-008's documented WP-010 replacement) | 3 (GREEN) | done | — |
| 9 | GREEN: wire createTerminalBridge to use the WS transport when configured; else the existing NOT_PTY_SESSION fallback (WPF-09 DI; production-safe) | 3 (GREEN) | done | — |
| 10 | GREEN: e2e harness WS->AF_UNIX terminal proxy (apps/cockpit/e2e/terminal-proxy.ts) bridging browser WS to real Python SocketServer over a real pty fake child; started from run-server.ts (harness only) | 3 (GREEN) | done | — |
| 11 | GREEN: implement test_terminal_lifecycle.py (restart + evict + aggregated regression gate) | 3 (GREEN) | done | — |
| 12 | GREEN: implement live-terminal.spec.ts journey so all four UI cases pass; full cockpit vitest + backend suites green (regression) | 3 (GREEN) | done | — |
| 13 | BLUE: bootstrap-from-zero (TDD 6.3) — record a clean run of backend suite + cockpit suite + this e2e | 4 (BLUE) | done | Bootstrap-from-zero recorded: backend 24/24, cockpit vitest 379/379, live e2e 4/4 — clean worktree, no external dep (pty fake child is a local script). e2e drives the REAL interface (Playwright→WS→AF_UNIX→SessionManager→pty). |
| 14 | BLUE: extract duplicated WS/transport plumbing at 2-consumer threshold; confirm e2e drives the REAL interface; read-only-inventory gate green | 4 (BLUE) | skipped | 2-consumer rule already taken: the lifecycle test + e2e backend reuse the shared session_child_adapters (3rd consumer) — no re-paste. NDJSON line-framing appears in the proxy (Node net.Socket buffering), the WS transport (browser WS-frame split), and the Python test _Client — but these are three different transports/languages; a forced shared abstraction would couple browser-only and Node-only code (anti-pattern). e2e drives the REAL interface confirmed; read-only-inventory gate green. |
| 15 | DOCS: update apps/cockpit/README.md terminal/e2e section (live-terminal e2e + WS proxy harness + bootstrap run) | 5 (DOCS) | done | — |
| 16 | Run lint + typecheck + format | 6 (LINT) | done | — |
| 17 | Code-review gate (/code-review on the diff) | 6.5 (REVIEW) | done | — |
| 18 | Commit (Conventional Commits) + push feat/wp-010-end-to-end-round-trip | 7 (COMMIT) | pending | — |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 1 | 2026-06-07T15:36:07Z | 2026-06-07T15:36:12Z | Worktree off change branch @954a1ab; journal init; npm workspace installed; baseline green (cockpit 379, backend socket+contract 21); preflight recorded (ws absent for e2e proxy) |
| 2 | 2026-06-07T15:37:07Z | 2026-06-07T15:43:00Z | RED: live-terminal.spec.ts (4 cases) FAIL — terminal not live-wired (notYetWiredTransport → no-terminal state) + harness port-collision diagnosed. test_terminal_lifecycle.py (3 cases: restart-recreates-pty, idle-pty-evicted, regression gate) PASS as expected — aggregation gate over already-merged backend WP-003/004/005/006; the genuine RED is the e2e live wiring. |
| 3 | 2026-06-07T15:43:06Z | 2026-06-07T16:04:40Z | GREEN: live terminal wired (WS transport + e2e WS↔AF_UNIX proxy + isomorphic base64 + open-spec injection). All 4 e2e cases pass through the REAL interface/socket/pty child. test_terminal_lifecycle.py 3/3. Regression: cockpit vitest 379/379, backend socket+contract+viewer 33/33, read-only gate clean. |
| 4 | 2026-06-07T16:04:46Z | 2026-06-07T16:06:11Z | BLUE: bootstrap-from-zero recorded (backend 24/24 + cockpit 379/379 + live e2e 4/4, clean worktree, no external dep). e2e drives the REAL interface (done=wired). Refactor: reused session_child_adapters (3rd consumer); no forced cross-transport abstraction. read-only gate green. |
| 5 | 2026-06-07T16:06:17Z | 2026-06-07T16:07:27Z | DOCS: apps/cockpit/README.md — added 'Live-terminal round-trip' e2e section (test:e2e:terminal, the WS↔AF_UNIX harness, bootstrap-from-zero) + script-table rows. |
| 6 | 2026-06-07T16:07:32Z | 2026-06-07T16:08:42Z | LINT: cockpit eslint clean, tsc (server+client) clean, prettier clean (fixed 1 file), ruff clean on test_terminal_lifecycle.py + terminal-backend.py. |
| 6.5 | 2026-06-07T16:08:48Z | 2026-06-07T16:11:29Z | addressed: /code-review bundle produced (REVIEW.md + signals.json + tool-outputs). Verdict 'Approve with fixes'. 0 critical/high, 1 medium (A-01: WS sidecar correctly harness-only, read-only gate green, production sidecar out of WP-010 scope — documented), 2 low (Q-01 line-framing benign for harness; Q-02 backend-orphan-on-interrupt already mitigated via clean EADDRINUSE reject + teardown). No unaddressed findings; all resolved/documented/watch-listed per CR-04. Bundle: .architecture/interactive-terminal-sessions/code-reviews/PR-feat-wp-010-end-to-end-round-trip-2026-06-07T160924Z/ |
| 7 | 2026-06-07T16:11:35Z | (in flight) | — |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|
| 2 | 1 | live-terminal.spec.ts (and even the existing happy-path.spec.ts) failed: ThreadView shows 'gone or moved'; dashboard listed 15 REAL ~/.sulis changes, not the seeded fixture. | A founder cockpit dev server is already LISTENING on 5173/5174; Playwright's reuseExistingServer:!CI reused it, so the e2e ran against the real ~/.sulis state dir instead of the seeded temp fixture. Environment collision, not a code defect — happy-path fails the same way. | RED confirmed (4/4 live-terminal cases fail). Plan: give the live-terminal e2e its OWN dedicated ports (+ proxy port) so it never collides with a running cockpit — an in-scope hardening of the harness I am building. Will verify GREEN against the isolated ports. | RED established; collision diagnosed; isolation fix scoped for GREEN. |
| 3 | 2 | Live e2e: component open() threw 'UNEXPECTED: provider'; then 'Buffer is not defined'; then 4 axe color-contrast violations on the live terminal chrome. | Three genuine LIVE-only wiring defects the merged code never exercised in a real browser: (1) WP-007 client open() sends only {io_mode:pty}; the socket _open requires provider+cwd. (2) WP-007 port's base64 used Node-only Buffer — undefined in the browser. (3) WP-008/WP-VISUAL chrome (connected badge white-on-green 3.3:1, meta/kbd muted-fg 4.34:1) just missed WCAG AA at 11px — only the live 'connected' state exposed it. | (1) e2e proxy injects resolved provider+cwd into open params (the production sidecar's change→spec job) — manager.open is idempotent so the pre-seeded session returns as-is. (2) Made TerminalBridge.ts base64 isomorphic (atob/btoa, browser+Node). (3) LiveTerminal.module.css: badgeConnected→--foreground (5.44:1, matches warning badge's dark-on-fill), meta+inputHint→--foreground — all token-respecting (WPF-07), no shared-token change. | All 4 e2e GREEN; cockpit vitest 379/379; backend 36/36; read-only gate clean. |

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

