# Executor journal — WP-005

> Started: 2026-06-02T08:10:28Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| python3 | present | — |
| pytest (uv) | present | — |
| PyYAML | present | — |
| actionlint | absent | manual YAML-shape analysis at Blue; PyYAML parse assertion covers structural validity |
| yamllint | absent | PyYAML safe_load parse assertion in test_marketplace_shim.py covers YAML validity |

## Plan

**Approach.** Substitute-Replace primitive (n=1 dogfood). Replace the marketplace's own 280-line .github/workflows/release-on-merge.yml wholesale with a thin shim that calls the plugin's reusable workflow via a LOCAL path ref (uses: ./plugins/sulis/templates/workflows/release-on-merge.yml — NOT the cross-repo @tag form, because the marketplace owns the workflow and a local ref always tracks the current commit). Key risk + load-bearing: the loop-guard (skip the bot's own release commits) must survive the shim->reusable indirection; head_commit.author + github.actor context propagate to reusable workflows called via workflow_call from a push trigger, so the job-level if: guard in the reusable workflow still fires. Preserve concurrency group + trigger exactly.

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | Write test_shim_is_thin (file <= ~30 lines, not the 280-line workflow) in plugins/sulis/scripts/tests/unit/test_marketplace_shim.py | 2 (RED) | done | — |
| 2 | Write test_uses_local_reusable_workflow_path (uses: ./plugins/sulis/templates/workflows/release-on-merge.yml) in test_marketplace_shim.py | 2 (RED) | done | — |
| 3 | Write test_trigger_is_push_main (on: push: branches: [main]) in test_marketplace_shim.py | 2 (RED) | done | — |
| 4 | Write test_secrets_inherit_present in test_marketplace_shim.py | 2 (RED) | done | — |
| 5 | Write test_loop_guard_preserved (passed through reusable OR present at shim level) in test_marketplace_shim.py | 2 (RED) | done | — |
| 6 | Write test_yaml_parses (PyYAML safe_load) in test_marketplace_shim.py | 2 (RED) | done | — |
| 7 | Replace .github/workflows/release-on-merge.yml wholesale with the thin local-ref shim + n=1 dogfood header | 3 (GREEN) | done | — |
| 8 | Verify loop-guard survives shim->reusable indirection (head_commit author context available via workflow_call from push); document the verification | 3 (GREEN) | done | Loop-guard verified to survive shim->reusable indirection: reusable workflow on: workflow_call carries job-level if: github.event.head_commit.author.username != 'github-actions[bot]' && github.actor != 'github-actions[bot]' (line 103); GitHub propagates the push event context into workflow_call-invoked reusable workflows, so the guard fires. No shim-level duplication needed. |
| 9 | Refactor / dedup review (conditional: 2-consumer threshold) | 4 (BLUE) | skipped | No refactor fired. The shim is 12 executable lines, already minimal. EP-03 2-consumer check: the shim shape resembles the consumer template, but they differ in the load-bearing local-ref-vs-@tag dimension — extracting a shared primitive would couple owner+consumer and defeat the distinction this WP establishes. The _LOCAL_USES string appears in 2 test modules, but hoisting one literal to a conftest adds indirection for negligible gain and breaks pytest module independence (established convention). Conscious skip. |
| 10 | YAML lint / parse verification (PyYAML; actionlint/yamllint absent -> manual shape analysis) | 6 (LINT) | done | ruff clean on 3 changed test files; YAML PyYAML-parses + passes manual actionlint-equivalent shape checks (valid reusable-workflow caller job: uses+secrets+permissions, no conflicting steps/runs-on). actionlint/yamllint absent locally; fallback applied. |
| 11 | Run /code-review gate against local diff | 6 (LINT) | done | Bundle at .architecture/auto-back-merge-on-release/code-reviews/PR-feat-wp-005-mkt-shim-2026-06-02T082225Z/. Verdict PASS: 0 critical/high/medium, 1 low (conscious-skip, no delta). REVIEW.md + signals.json + tool-outputs present. |
| 12 | Commit (Conventional Commits, n=1 dogfood body) + push | 7 (COMMIT) | pending | — |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 2 | 2026-06-02T08:11:27Z | 2026-06-02T08:12:41Z | 6 tests written in tests/unit/test_marketplace_shim.py; 3 shape tests fail RED (thin/local-uses/secrets-inherit), 5 preserve-invariant smoke tests pass (parse, trigger, loop-guard inline, reusable resolvable) |
| 3 | 2026-06-02T08:12:46Z | 2026-06-02T08:19:26Z | Marketplace .github/workflows/release-on-merge.yml replaced wholesale with a thin local-ref shim (uses: ./plugins/sulis/templates/workflows/release-on-merge.yml, secrets: inherit, push:[main] trigger + concurrency preserved). 8 shim tests pass; 4 sibling WP-002/003 characterisation tests re-pointed to the reusable workflow; full unit suite 1616 passed. Loop-guard survives the indirection. |
| 4 | 2026-06-02T08:19:34Z | 2026-06-02T08:20:00Z | No refactor fired (conscious skip, rationale in plan item 9). Shim is minimal 12 exec lines; no shared primitive to extract without defeating the local-ref-vs-tag distinction. Tests stay green (18 passed). |
| 5 | 2026-06-02T08:20:06Z | 2026-06-02T08:20:43Z | No-op. WP has no docs_to_update frontmatter. Behaviour change (workflow runs via reusable indirection) is documented inline in the shim header comment (n=1 dogfood + local-ref-vs-tag + loop-guard survival). GIT-12 (git-workflow-standard.md, WP-008) already describes the post-shim reusable+shim mechanism correctly; release-train SKILL.md (WP-006) references release-on-merge behaviourally (preserved). No marketplace-doc describes the 280-line body in a way the shim invalidates. |
| 6 | 2026-06-02T08:20:47Z | 2026-06-02T08:21:18Z | ruff check clean on all 3 changed test files; shim YAML PyYAML-parses and passes manual actionlint-equivalent shape validation (valid reusable-workflow caller job). No type checker configured for these files (pure-stdlib+pyyaml tests). |
| 6.5 | 2026-06-02T08:21:22Z | 2026-06-02T08:24:01Z | addressed: /code-review verdict PASS. 0 critical/high/medium findings; 1 low (the _LOCAL_USES constant duplicated across 2 test modules) is a documented conscious-skip (pytest module independence > one-literal DRY), not a defect — no inline fix or remediation WP needed. Bundle: .architecture/auto-back-merge-on-release/code-reviews/PR-feat-wp-005-mkt-shim-2026-06-02T082225Z/ (REVIEW.md + signals.json + tool-outputs verified present). |
| 7 | 2026-06-02T08:24:06Z | (in flight) | — |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|
| 3 | 1 | test_shim_is_thin failed: shim has 47 total lines (> 30 ceiling) | Total-line ceiling counted the required documentation header (n=1 dogfood + local-ref-vs-tag + loop-guard survival, all DoD-mandated). The regression the test must catch is the EXECUTABLE body ballooning back to 280 lines, not header prose. | In-scope (own test file): measure non-comment/non-blank executable lines; ceiling 20 (orig had 200+ exec lines, shim has ~12) | All 8 tests pass; thinness signal now correctly tracks executable body size |
| 3 | 2 | 4 sibling-WP tests broke after the Replace: test_release_on_merge_yaml_annotations.py (yaml_safe_loads name assert, step-annotation present, loop-guard conjunction) + test_reusable_workflow_byte_parity.py (byte-parity-with-marketplace) | These are WP-002/WP-003 characterisation tests (authored on THIS change branch + on dev) that pinned the marketplace .github/workflows/release-on-merge.yml to the pre-shim 280-line body. WP-005 is the documented point (per their own docstrings: 'should remain in place until WP-005') at which that body becomes a shim and the annotations+loop-guard relocate to the reusable workflow. In-scope: I am modifying the file these tests target. | Re-pointed both tests to the reusable workflow (where annotations+guard now live): updated path resolver, name assertion (release-on-merge -> startswith), retired the obsolete byte-parity-with-marketplace assertion (transitional-by-design) and replaced it with a shim-delegates-to-reusable assertion. Loop-guard conjunction regression armour preserved, now pinned in the reusable workflow. | 1616 unit tests pass; loop-guard infinite-loop protection intact. Pre-existing methodology .sh failure (WP-003 back-merge steps absent from WP-002 snapshot) confirmed pre-existing via stash test + out of WP-005 scope (diffs reusable vs snapshot, neither is WP-005's file). |

## Post-deploy verification

Security-reviewer verdict: PASS
Recorded at: 2026-06-02T08:38:52Z

## Notes

