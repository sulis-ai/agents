# Executor journal — WP-003

> Started: 2026-06-02T07:50:24Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| python3/pytest (via uv) | present | — |
| pyyaml | present | — |
| actionlint | absent | pyyaml safe_load parse check in test_back_merge_step.py covers YAML validity; actionlint runs in CI (sandbox) per TDD §9.5 |
| gh CLI | present | — |

## Plan

**Approach.** EXPAND-Extend primitive (back-merge-step-block scaffold). Append three net-new steps (pin-read, decide+act, post-condition) to the existing release job in plugins/sulis/templates/workflows/release-on-merge.yml, all gated on steps.detect.outputs.skip != 'true' (except post-condition which uses if: always()). No characterisation test needed — existing steps are untouched (WP-002 proved pre-extension behaviour). Key risk: canonical-string drift — LABEL/TITLE_PREFIX/base/head and the pin regex MUST match drift_check.sh + ADR-005/006 byte-for-byte (WP-009 enforces parity); and the version output is steps.compute.outputs.new_meta, NOT the WP example's steps.bump.outputs.new_version. No-force-push invariant is load-bearing (TDD §5.2).

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | Write static-assertion tests in plugins/sulis/scripts/tests/unit/test_back_merge_step.py (step-present+skip-gated, pin-read regex match/reject, ff uses --ff-only path, raced PR canonical LABEL+TITLE_PREFIX parity with drift_check.sh, no --force/--force-with-lease, post-condition present) | 2 (RED) | done | — |
| 2 | Implement pin-read step in release-on-merge.yml (gh api commits/{SHA}/pulls, regex dev-sha-at-open: ([a-f0-9]{40}), safe-default empty) | 3 (GREEN) | done | — |
| 3 | Implement decide+act step (ff dev->main via git push origin main:dev on pin==current-dev; else raced-path gh pr create --base dev --head main --label back-integrate + gh pr merge --auto) | 3 (GREEN) | done | — |
| 4 | Implement post-condition step (if: always(); dev==main OR open/merged back-integrate PR else exit 1) | 3 (GREEN) | done | — |
| 5 | Add no-force-push guard comment making the invariant explicit | 3 (GREEN) | done | — |
| 6 | Refactor: GH expressions through env: ports, single-line grep-friendly logs, canonical-string parity with drift_check.sh, actionlint-clean shape | 4 (BLUE) | done | Removed redundant GITHUB_REPOSITORY/COMMIT_SHA env re-declarations (auto-provided by runner); confirmed all GH expressions via env: ports, single-line logs, NEW_META=steps.compute.outputs.new_meta, GH_TOKEN never echoed. Cross-step bash extraction declined — workflow steps are separate shells; the two-step single-responsibility design is the boring convention (WP Notes). |
| 7 | Docs check — docs_to_update not set; auto-detect from modified source files | 5 (DOCS) | done | No-op: docs_to_update unset. GIT-12 standards doc (WP-007) and README shim section (WP-006) are separate WPs' scope; the workflow YAML is self-documented via inline comments. Nothing in THIS WP's file scope needs a doc edit. |
| 8 | Run YAML parse (pyyaml) + full unit suite green | 6 (LINT) | done | py_compile clean (the project's CI lint gate per branch-ci.yml), ruff check clean, YAML safe_load OK. Did NOT apply ruff format — project CI does not enforce it (lint = manifest JSON validity + py_compile); imposing it would diverge from repo convention (CP-01). |
| 9 | Commit + push | 7 (COMMIT) | in-progress | — |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 2 | 2026-06-02T07:51:26Z | 2026-06-02T07:52:42Z | RED: 11 workflow-content assertions fail (back-merge steps absent), 8 pure-regex + no-force assertions pass. Failures for the right reason — steps don't exist yet. |
| 3 | 2026-06-02T07:52:45Z | 2026-06-02T07:57:30Z | GREEN: 3 back-merge steps (pin/backmerge/post-condition) appended; test_back_merge_step.py 19/19 green; full unit suite 1593 passed. 2 in-scope self-heals: (1) guard-comment + grep regex reworded to canonical form; (2) byte-parity characterisation relaxed to prefix-parity to admit the documented extension (first 377 lines byte-identical — pure append). |
| 4 | 2026-06-02T07:57:35Z | 2026-06-02T07:58:24Z | BLUE: GH expressions confined to env: ports (dropped redundant auto-provided var re-declarations); single-line grep-friendly logs; canonical strings sourced-parity; no secrets logged. YAML parses (15 steps); 29 related tests + no-force static all green. No cross-step extraction (separate shells; CP-01 boring two-step design). |
| 5 | 2026-06-02T07:58:30Z | 2026-06-02T07:58:39Z | No-op: no docs in this WP's file scope. GIT-12 (WP-007) + README shim (WP-006) are other WPs; workflow is self-documented via inline comments. |
| 6 | 2026-06-02T07:58:44Z | 2026-06-02T07:59:20Z | LINT: py_compile clean (CI lint gate), ruff check clean, YAML safe_load OK. Full unit suite 1593 passed earlier this session. |
| 6.5 | 2026-06-02T07:59:25Z | 2026-06-02T08:01:16Z | addressed: /code-review verdict PASS, 0 findings. Bundle at .architecture/auto-back-merge-on-release/code-reviews/PR-feat-wp-003-backmerge-step-2026-06-02T075952Z/ (REVIEW.md + signals.json + tool-outputs). Mechanical floor clean (py_compile/ruff/yaml); all 3 lenses nothing-surfaced; no force-push token; GH exprs via env: ports; GH_TOKEN never echoed. |
| 7 | 2026-06-02T08:01:23Z | (in flight) | — |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|
| 3 | 1 | test_no_force_flags + test_workflow_contains_canonical_pin_regex failed | (a) no-force guard COMMENT contained the literal forbidden tokens --force/--force-with-lease, tripping the substring static scan (correct scan behaviour); (b) grep pattern used dev-sha-at-open: [a-f0-9]{40} but canonical TDD §3/ADR-006 form is the parenthesised dev-sha-at-open: ([a-f0-9]{40}) | Reword the guard comment to describe the invariant without writing the literal flags; switch the grep pattern to the canonical parenthesised regex | pending re-run |
| 3 | 2 | test_reusable_workflow_byte_parity: line-count diff marketplace=377 reusable=522 (first 377 lines matched byte-for-byte) | WP-002 characterisation test asserts the reusable workflow is byte-identical to the marketplace copy modulo trigger/name/permissions. WP-003's extend primitive deliberately appends the 145-line back-merge block beyond that copy — the strict byte-parity invariant is superseded by this WP's existence. The first 377 lines (the shared move-preserved portion) STILL match exactly, confirming WP-003 only appended and did not edit existing steps. | In-scope (same file under test, EP-07): relax the byte-parity assertion to PREFIX-parity — assert the marketplace workflow's lines match the reusable workflow byte-for-byte up to the WP-003 back-merge block marker, then allow the documented extension below it. Preserves WP-002's move-was-preserving characterisation; admits WP-003's extension. | pending re-run |

## Post-deploy verification

Security-reviewer verdict: PASS
Recorded at: 2026-06-02T08:09:16Z

## Notes

