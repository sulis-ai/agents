# Executor journal — WP-004

> Started: 2026-06-25T22:21:07Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| node/npm | present | — |
| python3/pytest | present | — |
| playwright chromium | present | chromium headless shell cached in ms-playwright; node_modules need npm install |
| node_modules | absent | run npm install in wp-004-worktree before tests/build |

## Plan

**Approach.** Create/integration (seam-close, CF-07/CF-12). Close two folded security concerns and drive the 5 authored Scenarios green over the real round-trip. DAT-PERSIST-01: nothing appends chat turns to the per-product thread today, so getThread returns nothing — add append_turn to the Python chat_scope_store (via LocalThreadStore.append_message redaction-on-write) + an appendTurn on the TS ChatScopeStore port/adapter routed through that Python redacting path, and call it from the message route (user before relay, assistant after). ADV-CWD-01: ground the chat-scope relay cwd to the scope's real chat-store root (today resolveChange(scope) is null -> cwd:''). Key risk: redaction must stay single-source (Python catalogue, 347 lines of regex) — do NOT port to TS; persist via the Python path.

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | RED: test_append_turn_persists_redacted in test_chat_scope_store.py — append a user+agent turn via chat_scope_store.append_turn; get_messages returns them; a secret in content is [redacted-secret] on disk | 2 (RED) | pending | — |
| 2 | RED: chat-scope-store.contract.test.ts — appendTurn round-trips through getThread (user+assistant messages returned in order), isolated per scope | 2 (RED) | pending | — |
| 3 | RED: chatScope route message test — POST /:scope/message persists the prompt + reply to the scope thread (store.appendTurn called for user before relay and assistant after) | 2 (RED) | pending | — |
| 4 | RED: chatScope route — relay cwd is grounded to a real dir for the scope (not '') — bridge resolve receives a real cwd | 2 (RED) | pending | — |
| 5 | RED: e2e product-wide-chat.spec.ts — encode Scenarios 1-5 (switch swaps board+chat+history; talk->card; pick/switch agent guard; overview asks which product; a11y) | 2 (RED) | pending | — |
| 6 | GREEN: add append_turn(chat_scope, role, content, ...) to chat_scope_store.py via LocalThreadStore.append_message (redaction-on-write) | 3 (GREEN) | pending | — |
| 7 | GREEN: add appendTurn to ChatScopeStore port + LocalChatScopeStore adapter (persist via Python redacting path) | 3 (GREEN) | pending | — |
| 8 | GREEN: wire message route to appendTurn(user) before relay + appendTurn(assistant) after relay completes; capture reply text from sink | 3 (GREEN) | pending | — |
| 9 | GREEN: ground the chat-scope relay cwd to the scope's real chat-store root (create dir); fix resolveChange(scope) null->cwd:'' path | 3 (GREEN) | pending | — |
| 10 | GREEN: CF-07 conformance — client funnel <-> server route shapes agree end-to-end; record the check | 3 (GREEN) | pending | — |
| 11 | GREEN: drive the 5 scenarios; record observed-or-blocked evidence on the scenario files | 3 (GREEN) | pending | — |
| 12 | BLUE: extract shared turn-projection / redaction helper if 2-consumer threshold fires; no client-side board filtering; honest running-provider check | 4 (BLUE) | skipped | No 2-consumer duplication fired; grounding single-source in LocalChatScopeStore.groundCwd; redaction single-source (Python catalogue). Blue invariants verified: no client-side board filtering (dock reads useActiveProduct only; ADR-004); honest running-provider (running=chat.provider, overlay cleared on refetch); post-build visual = token-only CSS, dock mounted unchanged. |
| 13 | DOCS: update any module headers / contract notes touched by the seam-close (no separate docs file expected) | 5 (DOCS) | pending | conditional |
| 14 | Run lint + typecheck + format (server + client) | 6 (LINT) | pending | — |
| 15 | Step 6.5 code-review gate against local diff | 6.5 (REVIEW) | pending | — |
| 16 | Commit + push | 7 (COMMIT) | pending | — |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 2 | 2026-06-25T22:23:22Z | 2026-06-25T22:30:39Z | RED: 4 Python append_turn tests (redaction-on-write), 4 TS store-contract tests (appendTurn round-trip + groundCwd), 3 route persistence tests, and e2e product-wide-chat.spec.ts (5 scenarios) all fail for the right reasons (append_turn/appendTurn/groundCwd absent; route does not persist; dock not mounted). |
| 3 | 2026-06-25T22:30:44Z | 2026-06-25T22:56:52Z | GREEN: append_turn added to Python chat_scope_store (redaction-on-write via LocalThreadStore.append_message) + new vendored sulis-chat-append CLI; appendTurn+groundCwd added to ChatScopeStore port + LocalChatScopeStore (persists via the Python redacting path, single-source secret catalogue); message route now persists user-before-relay + assistant-after-relay and grounds cwd; index.ts grounds chat-scope relay cwd to the real scope dir; filename convention aligned to thread-id (chat.*.jsonl) to match the Python writer; ProductChatDock mounted in WorkspaceShell (board central, chat docked right). 5 authored Scenarios DRIVEN GREEN over the real interface (e2e product-wide-chat.spec.ts, all 5 pass; full 17-test e2e smoke green). Fixed 3 real AA contrast violations the a11y gate caught (slash-hint, idle status, bubble timestamp). Server targeted suites 76/76, Python 15/15, client dock 22/22. CF-07 conformance held (shared contract test). |
| 4 | 2026-06-25T22:56:59Z | 2026-06-25T22:58:01Z | BLUE: no 2-consumer duplication fired in new code (grounding single-source in groundCwd; redaction single-source in Python catalogue; resolvePluginScriptsDir reused for script resolution). Blue DoD invariants verified: (1) no client-side board filtering — dock reads useActiveProduct() only, server scope owns the board (ADR-004); (2) honest active-agent identity — AgentPicker running=chat.provider sourced from the server thread, optimistic overlay cleared on refetch (AI-07); (3) post-build visual = token-only CSS (no raw hex in property values), the signed-contract dock mounted unchanged. All tests remain green after refactor review. |
| 5 | 2026-06-25T22:58:10Z | 2026-06-25T22:58:46Z | DOCS: no docs_to_update frontmatter; the new vendored sulis-chat-append CLI is self-documenting (full module docstring). Updated the LocalChatScopeStore.ts header (doc-in-code) to reflect the closed round-trip: corrected the stale filename convention (now thread-id-keyed chat.*.jsonl) and the stale 'provider stamp is the ONE write' claim (now documents three writes: rememberProvider / appendTurn-via-redacting-CLI / groundCwd). No separate docs file applies. |
| 6 | 2026-06-25T22:58:55Z | 2026-06-25T23:00:55Z | LINT/TYPE/FORMAT clean: tsc --noEmit (server+client) passes after fixing TranscriptMessage narrowing in the contract test (added a typed messageText helper); eslint --ext .ts,.tsx clean; ruff check clean on sulis-chat-append + chat_scope_store.py + the Python test; py_compile OK. Formatting covered by eslint (no separate format script). |
| 6.5 | 2026-06-25T23:01:03Z | 2026-06-25T23:04:42Z | addressed: /code-review bundle written at .architecture/product-wide-chat/code-reviews/PR-feat-wp-004-integration-seam-close-2026-06-25T230305Z/ (REVIEW.md + signals.json + tool-outputs). Verdict PASS. 3 lenses ran: architecture 0, security 0, quality 1. The one medium finding (F-01: user-turn appendTurn+groundCwd awaited unguarded → a transient durable-write failure would 500 the chat; assistant-turn was already fail-soft) FIXED INLINE (wrapped in matching fail-soft try/catch); re-ran route+contract+adapter suites 42/42 green, typecheck+lint clean. 0 remaining findings; 0 remediation WPs; 0 exceptions. |
| 7 | 2026-06-25T23:04:50Z | 2026-06-25T23:05:56Z | Pushed to wp/create-product-wide-chat/wp-004-integration-seam-close at SHA c494381423d75b9589152b6fc012a840704984dd. Commit feat(wp-004) created with pre-commit hook passing (no --no-verify); origin stamp degraded to inferred (run-ulid not in env, non-fatal per ADR-013). Remote branch created; CI triggered. Calling session can proceed with Step 8+ (wpx-pipeline). |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

