# Executor journal — WP-001

> Started: 2026-06-09T20:07:28Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| uv run pytest (9.0.3) | present | — |
| ruff (0.15.14) | present | — |
| coverage | absent | manual coverage check at Step 4 (new files only; pytest-cov not in dev group) |

## Plan

**Approach.** Create primitive (EXPAND). Build _drive_specify.py — a deterministic non-interactive harness that drives the real specify path (reuses classify_depth from _specify_classifier.py) on a fixture change manifest, with --depth forced as an explicit input (no founder negotiation), and writes a comprehensive document to --out. Ships three minimal fixtures. Key risk: the comprehensive-doc emitter is itself deferred to WP-006, so the driver reuses the currently-available specify scaffold (depth classifier + canonical section structure) without forking document emission. The Blue cross-WP fixture-helper extraction with WP-002 is out-of-scope here (peer in a separate worktree) and will be skipped.

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | Write test_drive_specify_emits_document_at_lite in plugins/sulis/scripts/tests/test_drive_specify.py (drives sample-user-facing at lite, asserts a file at --out) | 2 (RED) | done | — |
| 2 | Write test_drive_specify_deterministic_same_fixture_same_depth in plugins/sulis/scripts/tests/test_drive_specify.py (two runs => byte-identical section set, NFR-04) | 2 (RED) | done | — |
| 3 | Write test_drive_specify_nonzero_on_stage_failure in plugins/sulis/scripts/tests/test_drive_specify.py (broken fixture => non-zero exit) | 2 (RED) | done | — |
| 4 | Create the three fixtures under plugins/sulis/scripts/tests/fixtures/methodology/ (sample-user-facing, no-dependencies, sample-tool-surface) | 3 (GREEN) | done | — |
| 5 | Implement _drive_specify.py in plugins/sulis/scripts/ (parses --fixture/--depth/--out, reuses _specify_classifier real specify path, writes document, exit 0 on success / non-zero on stage failure) | 3 (GREEN) | done | — |
| 6 | Extract shared fixture-loading helper with _drive_journey_walk.py (WP-002) if both read the same manifest shape | 4 (BLUE) | skipped | 2-consumer threshold (EP-03) not reached: _drive_journey_walk.py (WP-002) is a parallel peer in a separate worktree and does not exist on this branch. _drive_specify.py is the sole consumer of the fixture-manifest shape here, so extracting a shared helper now would be speculative (YAGNI) and risk a cross-WP file collision. No intra-file duplication found; code is already explicit/boring per boring-code.md. The extraction is correctly deferred to the WP that lands the second consumer. |
| 7 | Update docs if any apply (no docs_to_update frontmatter; harness is internal scripts) | 5 (DOCS) | done | Updated tests/README.md: added fixtures/methodology/ to the layout tree and a 'Methodology harness' section documenting _drive_specify.py + the three fixtures + a usage example. No docs_to_update frontmatter; this is the natural doc surface for the new harness. |
| 8 | Run ruff check + format on _drive_specify.py and test_drive_specify.py | 6 (LINT) | done | — |
| 9 | Run /code-review against the local diff vs change branch | 6.5 (REVIEW) | done | /code-review run: bundle at .architecture/comprehensive-spec-and-journey-walk/code-reviews/PR-feat-wp-001-drive-specify-harness-2026-06-09T201845Z/ (REVIEW.md + signals.json + tool-outputs). Verdict PASS, 0 findings. |
| 10 | Commit (Conventional Commits) + push feat/wp-001-drive-specify-harness | 7 (COMMIT) | pending | — |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 2 | 2026-06-09T20:08:20Z | 2026-06-09T20:09:32Z | RED: 6 tests in test_drive_specify.py all fail because _drive_specify.py does not yet exist (correct reason). Covers happy-path emit, determinism (NFR-04), broken-fixture non-zero exit, and all three named fixtures. |
| 3 | 2026-06-09T20:09:38Z | 2026-06-09T20:16:19Z | GREEN: _drive_specify.py + 3 fixtures created. 14/14 tests in test_drive_specify.py pass. Driver reuses the real specify path (classify_depth from _specify_classifier), forces --depth as explicit input, emits the 10-section canonical document with n/a-marking (ADR-002). Coverage of the new module ~100% of reachable lines (manual; coverage tool absent per preflight). Pre-existing AF_UNIX-path-too-long integration failures are out-of-scope (recorded in self-heal attempt 1). |
| 4 | 2026-06-09T20:16:23Z | 2026-06-09T20:16:50Z | BLUE: no refactor applied. The only Blue candidate (shared fixture-helper extraction with WP-002) is correctly deferred — 2-consumer threshold not reached on this branch (single consumer). No intra-file duplication; code is already explicit/boring. All 14 tests remain green; no behaviour introduced in Blue. |
| 5 | 2026-06-09T20:16:59Z | 2026-06-09T20:17:36Z | DOCS: updated plugins/sulis/scripts/tests/README.md — added fixtures/methodology/ to the layout and a 'Methodology harness' section (driver behaviour, the three fixtures, usage example). Module-level docstring in _drive_specify.py is comprehensive. No docs_to_update frontmatter. |
| 6 | 2026-06-09T20:17:40Z | 2026-06-09T20:18:07Z | LINT: ruff check passed (no errors) on _drive_specify.py + test_drive_specify.py; ruff format applied (test file reformatted, cosmetic). Suite re-run 14/14 green after format. |
| 6.5 | 2026-06-09T20:18:12Z | 2026-06-09T20:20:04Z | addressed: /code-review run, bundle produced at .architecture/comprehensive-spec-and-journey-walk/code-reviews/PR-feat-wp-001-drive-specify-harness-2026-06-09T201845Z/ (REVIEW.md + signals.json verified present). Verdict PASS — 0 findings across all three lenses; mechanical baseline clean (ruff check/format/import). 0 inline fixes, 0 remediation WPs, 0 exceptions. |
| 7 | 2026-06-09T20:20:08Z | (in flight) | — |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|
| 3 | 1 | Full pytest suite: 22 failed + 8 errors in tests/integration/test_socket_server.py and test_session_viewer.py | OSError: AF_UNIX path too long — the deep worktree path (/Users/iain/Documents/repos/wp-001-worktree/...) plus the per-test tmp socket path exceeds the macOS sun_path 104-byte limit. Pre-existing, environmental, in the socket/session integration subsystem; nothing in that subsystem imports _drive_specify. | None applied — scope guard fired: socket integration test infrastructure is outside this WP's Contract (Contract scope is _drive_specify.py + fixtures + test_drive_specify.py). The failure is independent of this WP's files. | WP-001's own suite (tests/test_drive_specify.py) is 6/6 green. The integration failures are out-of-scope and triggered by worktree path length, not by this change. |

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

