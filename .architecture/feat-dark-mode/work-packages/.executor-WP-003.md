# Executor journal — WP-003

> Started: 2026-06-07T20:28:06Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| node v20.20.2 / npm 10.8.2 | present | — |
| gh CLI | present | — |
| node_modules (vitest/eslint/tsc) | absent | npm ci at worktree root before Step 2 (committed package-lock.json present) |
| @vitest/coverage-v8 | absent | manual branch/line trace at Step 3: both new files fully exercised (resolveInitialTheme all branches; ThemeProvider all exports + persist success), ≥90% by inspection. Not in lockfile; not installable without adding a dep (out of scope). |

## Plan

**Approach.** EXPAND-Create primitive (new behaviour, no existing prior art in src/theme — confirmed absent). Build the React theme context layer per ADR-001: a pure resolveInitialTheme() seam (saved-localStorage wins over prefers-color-scheme; light fallback when either API throws), a ThemeProvider that sets documentElement.dataset.theme on mount + change and persists on toggle, and a useTheme() hook. Wire the provider around BrowserRouter in App.tsx only (AppRoutes stays bare so MemoryRouter tests keep mounting it). Scope is the mechanism only: NO toggle UI (WP-004), NO tokens.css edits (WP-002, parallel peer). Key risk: the storage-key + Theme type must be single source of truth for WP-004/WP-005 to import.

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | Write resolveInitialTheme.test.ts (saved-wins / OS-dark / OS-light / localStorage-throws / matchMedia-throws) in apps/cockpit/client/src/tests/theme/resolveInitialTheme.test.ts | 2 (RED) | done | — |
| 2 | Write ThemeProvider.test.tsx (mount OS-dark sets data-theme=dark / toggle flips + persists / saved-contradicts-OS wins on remount / useTheme outside provider throws) in apps/cockpit/client/src/tests/theme/ThemeProvider.test.tsx | 2 (RED) | done | — |
| 3 | Implement theme types + storage-key constant + resolveInitialTheme() in apps/cockpit/client/src/theme/resolveInitialTheme.ts | 3 (GREEN) | done | — |
| 4 | Implement ThemeProvider + useTheme() in apps/cockpit/client/src/theme/ThemeProvider.tsx | 3 (GREEN) | done | — |
| 5 | Wire ThemeProvider around BrowserRouter in apps/cockpit/client/src/App.tsx | 3 (GREEN) | done | — |
| 6 | Confirm Theme type + storage key are single-source-of-truth (extract to one module) so WP-004/WP-005 import not re-declare; verify no duplication | 4 (BLUE) | skipped | No duplication arose: Theme type + THEME_STORAGE_KEY declared once in resolveInitialTheme.ts and re-exported from ThemeProvider.tsx — single source of truth already by construction. WP-004/WP-005 import from one place. No extraction needed. |
| 7 | Confirm full existing suite (routing.test.tsx, Shell.test.tsx) stays green without requiring the provider | 4 (BLUE) | done | — |
| 8 | Docs: no founder-facing docs identified for theme mechanism (provider is internal infra; no docs_to_update frontmatter). No-op unless README references surface. | 5 (DOCS) | done | Added a theme/ line to the cockpit README directory map (the only doc surface the WP touches). No founder-facing doc / no docs_to_update frontmatter; the mechanism is internal infra. |
| 9 | Run lint + typecheck + format (eslint, tsc -p client, prettier) | 6 (LINT) | done | — |
| 10 | Run /code-review against local diff vs origin base; address findings | 6.5 (REVIEW) | done | — |
| 11 | Commit (Conventional Commits) + push feat/wp-003-theme-provider | 7 (COMMIT) | done | — |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 1 | 2026-06-07T20:34:27Z | 2026-06-07T20:34:36Z | Worktree feat/wp-003-theme-provider off change/feat-dark-mode @860b6df; npm ci installed deps; baseline client suite green in isolation (3 Monaco-test timeouts under full-suite parallel load are pre-existing flake, pass standalone) |
| 2 | 2026-06-07T20:35:31Z | 2026-06-07T20:39:23Z | RED: resolveInitialTheme.test.ts (7 cases) + ThemeProvider.test.tsx (9 cases) written; both fail on missing modules (correct reason) |
| 3 | 2026-06-07T20:39:34Z | 2026-06-07T20:49:21Z | GREEN: resolveInitialTheme.ts + ThemeProvider.tsx implemented, ThemeProvider wired around BrowserRouter in App.tsx. 15/15 theme tests pass (1 self-heal: throw-vs-absent distinction per TDD §5.1). Coverage ≥90% on new files by manual trace (coverage-v8 not installed). |
| 4 | 2026-06-07T20:49:28Z | 2026-06-07T20:53:12Z | BLUE: full client suite green (25 files / 123 tests). routing.test + Shell.test still pass without the provider (AppRoutes/Shell stay bare). No duplication to extract — Theme type + storage key single-sourced in resolveInitialTheme.ts, re-exported from ThemeProvider.tsx. |
| 5 | 2026-06-07T20:53:21Z | 2026-06-07T20:54:09Z | DOCS: added a theme/ entry to the cockpit README src directory map so it stays truthful with the new dir. No other doc surface applies (internal infra; no docs_to_update frontmatter). |
| 6 | 2026-06-07T20:54:16Z | 2026-06-07T20:59:39Z | LINT: typecheck clean (tsc server+client), eslint clean, prettier --write applied to 2 files (whitespace only); all touched files pass prettier --check; theme tests still 15/15 green post-format. |
| 6.5 | 2026-06-07T21:00:07Z | 2026-06-07T21:07:53Z | addressed: /code-review verdict PASS. Bundle at .architecture/feat-dark-mode/code-reviews/PR-feat-wp-003-theme-provider-2026-06-07T210311Z/ (REVIEW.md + signals.json + tool-outputs verified present). 0 critical/high/medium; 1 low (persistTheme write-throws path untested, defensive symmetry) → Watch List, no action, suggested for WP-004. No deltas. |
| 7 | 2026-06-07T21:08:06Z | 2026-06-07T21:09:43Z | Pushed to feat/wp-003-theme-provider at SHA d4ca7775f46e399102afcd3ff53e6d9f0f844e8c (off change/feat-dark-mode @860b6df). 6 files: resolveInitialTheme.ts + ThemeProvider.tsx + 2 specs + App.tsx wiring + README map line. CI triggered. Calling session can proceed with Step 8+. |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|
| 3 | 1 | resolveInitialTheme.test 'localStorage throws → light' got 'dark' | Impl conflated a throwing localStorage with an absent value: a throw fell through to OS preference. Contract/TDD §5.1 require a throw from EITHER browser API to short-circuit to 'light' (distinct from absent → OS). | Distinguish throw from clean-miss: signal a throw from readSavedTheme via a sentinel and force 'light' fallback on any API throw. | in-scope (own file resolveInitialTheme.ts); applying fix |

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

