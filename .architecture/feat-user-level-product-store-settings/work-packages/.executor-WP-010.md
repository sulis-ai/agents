# Executor journal — WP-010

> Started: 2026-06-09T12:59:30Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| python3 | present | — |
| vitest + supertest | present | resolved via symlinked node_modules from sibling change-worktree (gitignored) |
| _entity_adapter_local.py (vendored emitters) | present | — |

## Plan

**Approach.** Composite/Create — the graph-closing CF-07 conformance WP. Three touches: (1) wire WP-009's standalone forms/dialog into WP-008's SettingsPage (the open seam — affordance handlers were unset), invalidating SETTINGS_QUERY_KEY + PRODUCTS_QUERY_KEY on success so the tree+switcher refresh; (2) swap mock-for-real with an end-to-end conformance test driving the real settingsRouter + SpineSettingsAdapter over a mkdtemp brain (supertest), proving the SPEC acceptance incl. the disk-safety sentinel; (3) align the cosmetic .btn divergence to the form variant. Key risk: the e2e/wiring test must drive the real React state seam without re-mocking, and the conformance test must not pollute ~/.sulis (mkdtemp brain, same discipline as WP-005).

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | Write conformance RED: client_against_real_router_round_trip in apps/cockpit/server/tests/settings.conformance.test.ts (real settingsRouter + SpineSettingsAdapter + mkdtemp brain via supertest; full CRUD round-trip) | 2 (RED) | done | — |
| 2 | Write conformance RED: every_settings_error_shape_conforms in settings.conformance.test.ts (each SettingsErrorCode returned in ApiError envelope as typed) | 2 (RED) | done | — |
| 3 | Write conformance RED: new_product_appears_in_switcher_no_reload in settings.conformance.test.ts (created product shows in tree read with no reload) | 2 (RED) | done | — |
| 4 | Write conformance RED: remove_then_sentinel_survives_end_to_end against real router in settings.conformance.test.ts (disk-safety sentinel proven through real router+adapter) | 2 (RED) | done | — |
| 5 | Write e2e RED: Settings.e2e.test.tsx wiring assertions in apps/cockpit/client/src/pages/SettingsPage.wiring.test.tsx (clicking Add/Rename/Edit/Attach/Remove opens the right WP-009 form/dialog; onSuccess invalidates cache) | 2 (RED) | done | — |
| 6 | Wire WP-009 forms+dialog into SettingsPage: state machine for active form, render EntityForm/AttachRepoForm/ConfirmRemoveDialog, pass affordance handlers to ProductRow; onSuccess invalidates SETTINGS_QUERY_KEY + PRODUCTS_QUERY_KEY | 3 (GREEN) | done | — |
| 7 | Make conformance + e2e tests green against the real wiring (no mocks at the seam) | 3 (GREEN) | done | — |
| 8 | Blue: extract shared temp-brain + supertest router-mount harness reused by conformance test | 4 (BLUE) | done | 2-consumer threshold reached (conformance + e2e both shared tmpBrain/tmpFolder/realApp/availability gate); extracted to server/tests/helpers/settingsHarness.ts (TempDirs + realSettingsApp + realAdapter + adapterAvailable + resolveScriptsDir) |
| 9 | Align cosmetic .btn divergence: Settings.module.css .btn -> form variant (padding 8px 14px, --radius-interactive) | 4 (BLUE) | done | Settings.module.css .btn aligned to the form variant (padding 8px 14px, --radius-interactive); token already used 4x in-file so the choice is internally consistent |
| 10 | Docs: no-op check (no docs_to_update frontmatter; integration WP touches no doc file) | 5 (DOCS) | skipped | No-op: WP-010 has no docs_to_update frontmatter; the README API table (WP-006 routes) is unchanged (no new/changed routes or wire shapes); the behaviour change is UI-internal page wiring with no founder-facing doc describing it. No README 'not yet wired' claim to update. |
| 11 | Run lint + typecheck + format (eslint, tsc server+client) | 6 (LINT) | done | typecheck clean (server+client); eslint clean (added the standard 4-levels-deep no-restricted-imports disable to SettingsActions.test.tsx); prettier --write on 5 new files to match the repo's dominant formatted style |
| 12 | Code-review gate (/code-review) on the local diff | 6.5 (REVIEW) | done | /code-review run; bundle at .architecture/.../code-reviews/PR-feat-wp-010-...-2026-06-09T133113Z/ (REVIEW.md + signals.json + tool-outputs). Verdict PASS: 0 critical/high/medium, 1 low (per-render handlers alloc — benign, Watch List, no delta). Nothing to address. |
| 13 | Commit + push feat/wp-010-settings-integration-conformance | 7 (COMMIT) | in-progress | — |
| 14 | Add SettingsActions.test.tsx — deterministic unit coverage of all 7 overlay branches + invalidate-both-keys/close/cancel (no python; guarantees >=90% on the new wiring file) | 3 (GREEN) | done | discovered during implementation: e2e walks only 4 of 7 form kinds; coverage-v8 not installed so manual branch coverage needed |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 2 | 2026-06-09T13:01:28Z | 2026-06-09T13:06:30Z | RED: settings.conformance.test.ts (4 named CF-07/SPEC cases — pass against real wiring, pinning the producer) + Settings.e2e.test.tsx (4 wiring cases — fail correctly: affordance handlers unset, no form opens) |
| 3 | 2026-06-09T13:06:37Z | 2026-06-09T13:21:47Z | GREEN: wired WP-009 forms/dialog into SettingsPage via new SettingsActions.tsx (useSettingsActions + SettingsActionOverlay; onSuccess invalidates SETTINGS_QUERY_KEY+PRODUCTS_QUERY_KEY). Full suite 1239/1239 (x2 stable); read-only gate clean (240 files); contract audit clean. New file SettingsActions.tsx fully covered by SettingsActions.test.tsx (10 tests, all 7 branches) + e2e (4). |
| 4 | 2026-06-09T13:21:53Z | 2026-06-09T13:26:07Z | BLUE: extracted shared real-wiring harness (server/tests/helpers/settingsHarness.ts: TempDirs + realSettingsApp + realAdapter + adapterAvailable) reused by conformance + e2e at the 2-consumer threshold; aligned Settings.module.css .btn to the form variant (8px 14px / --radius-interactive). No behaviour added; full suite 1239/1239 green. |
| 5 | 2026-06-09T13:26:16Z | 2026-06-09T13:27:27Z | DOCS no-op: no docs_to_update; README API surface (WP-006 routes) unchanged by the frontend wiring; behaviour change is UI-internal with no founder-facing doc to update. |
| 6 | 2026-06-09T13:27:33Z | 2026-06-09T13:30:04Z | LINT: typecheck clean (tsc server+client), eslint clean, prettier clean on all changed files. One lint fix (no-restricted-imports disable on the 4-levels-deep wire-type import in SettingsActions.test.tsx, matching the sibling convention). |
| 6.5 | 2026-06-09T13:30:09Z | 2026-06-09T13:32:50Z | addressed: /code-review verdict PASS (bundle PR-feat-wp-010-...-2026-06-09T133113Z; REVIEW.md + signals.json present). 0 critical/high/medium; 1 low (per-render handlers allocation — benign, Watch List, no fix). No findings required action; no remediation WP, no exception. |
| 7 | 2026-06-09T13:32:57Z | (in flight) | — |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|
| 3 | 1 | Settings.e2e.test.tsx rename/attach cases flaked ~1-in-3: findByText timed out at TL's 1000ms default while the Save button was still disabled (busy) | Each write drives a real python emitter subprocess + a refetch via another real list subprocess; under the forked test pool that write->refetch round-trip exceeds Testing Library's 1000ms findBy/waitFor default | Added ROUND_TRIP={timeout:30000} and applied it to every findBy*/waitFor that waits on a write->refetch (in-scope test file); mirrors WP-005 adapter test's per-suite budget for the same real-subprocess work | pending re-run x4 |
| 3 | 2 | Full-suite run: read-only gate (check-read-only-script.test.ts) flagged client/src/pages/settings/Settings.e2e.test.tsx for writeFileSync + execFileSync; also an initial-GET findByRole timed out at 1000ms under max parallel load | (1) The e2e test, colocated at pages/settings/, was scanned as active source by the read-only gate, which excludes **/tests/** + e2e/ but not by .test filename; (2) the first-paint findByRole lacked the extended round-trip budget | Moved the e2e to client/src/tests/Settings.e2e.test.tsx (the gate-excluded home where the cockpit's other real-subprocess client tests live; fixed import depths) and applied ROUND_TRIP to the initial findByRole. SettingsActions.test.tsx (pure-mock, no write/process verbs) stays colocated. | gate test 34/34 green; e2e 4/4 green; full-suite re-run pending |

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

