# Executor journal — WP-003

> Started: 2026-06-08T11:00:43Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| pytest | present | — |
| ruff | present | — |
| mypy | present | — |
| claude binary | present | real interactive adapter wireable on founder machine; integration tests inject fake pty child (MEA-09) |

## Plan

**Approach.** Create primitive (EXPAND-Create). New session_manager_daemon.py: a singleton+stable-socket+idle-exit wrapper composing the frozen SessionManager+SocketServer and the WP-001 BindingManager/ConnectionBindingRegistry, registering the real InteractiveClaudePtyAdapter (WP-004) under provider 'pty'. Singleton via fcntl.flock(LOCK_EX|LOCK_NB) taken BEFORE bind so only the lock-holder reaches SocketServer.start's unlink+bind (closes the race ADR-001 names). Daemon-level idle-empty auto-exit via a clock-injected watcher, distinct from the engine's per-session idle eviction. Key risk: integration tests cannot run the real claude binary (WP-009 lesson) so the daemon exposes an injectable adapters seam (mirroring session_manager_host._build_server) for tests to wire PtyChildAdapter+fake child; production main() wires the real adapter. Independence MUST: zero chat/platform import.

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | Write test_first_daemon_takes_lock_serves_and_prints_ready in tests/integration/test_daemon_singleton.py | 2 (RED) | done | — |
| 2 | Write test_second_daemon_loses_race_exits_zero_without_clobbering_socket in tests/integration/test_daemon_singleton.py | 2 (RED) | done | — |
| 3 | Write test_sigterm_cleanly_stops_unlinks_socket_releases_lock in tests/integration/test_daemon_singleton.py | 2 (RED) | done | — |
| 4 | Write test_daemon_module_no_chat_or_platform_import in tests/integration/test_daemon_singleton.py | 2 (RED) | done | — |
| 5 | Write test_idle_watcher_fires_shutdown_after_n_empty_seconds in tests/unit/test_daemon_idle_exit.py (injected clock) | 2 (RED) | done | — |
| 6 | Write test_idle_watcher_does_not_fire_while_session_exists in tests/unit/test_daemon_idle_exit.py | 2 (RED) | done | — |
| 7 | Write test_idle_watcher_resets_empty_clock_when_a_session_appears in tests/unit/test_daemon_idle_exit.py | 2 (RED) | done | — |
| 8 | Run new tests; confirm they fail for the right reason (module/symbol absent) — Red | 2 (RED) | done | — |
| 9 | Implement IdleEmptyExitWatcher (injected clock + status_keys callable) in session_manager_daemon.py | 3 (GREEN) | done | — |
| 10 | Implement main(--socket) per Contract: flock LOCK_EX\ | LOCK_NB before bind; lock-holder is sole binder; race-loser confirms live socket and exits 0; wire real InteractiveClaudePtyAdapter under provider pty (injectable seam for tests); SocketServer with BindingManager+registry guard ON; print READY; idle-empty watcher; SIGTERM/SIGINT clean stop | done | pending |
| 11 | Retire session_manager_host.py standalone main per ADR-003 (shim — classes already moved in WP-001) | 3 (GREEN) | skipped | Host's standalone main is already a thin shim importing the WP-001-moved classes (the Contract's allowed 'one-line shim' option). NOT deleted: apps/cockpit/server/index.ts still spawns session_manager_host.py and the host unit+integration suites still exercise its main/_build_server until WP-007 migrates the cockpit onto the daemon. Deleting now is out-of-scope breakage WP-007 owns (ADR-003 supersession sequencing). |
| 12 | Run full related suite; confirm green + coverage on new file >=90% | 3 (GREEN) | done | — |
| 13 | Refactor: extract shared _build_server shape if 2-consumer threshold fires; verify frozen engine untouched; socket dir 0o700 | 4 (BLUE) | skipped | 2-consumer threshold not met for extraction. The genuine shared primitive (BindingManager + ConnectionBindingRegistry) is already extracted to _session_manager/binding.py (WP-001) and imported by both the host and the daemon. The remaining _build_server wiring is thin glue that DIVERGES in params (host carries bound/cwd/fake-child for its still-live test surface; daemon wires the real adapter + start_maintenance=True) and the host side is on the retiring path (WP-007). Extracting it would force editing the retiring host across the supersession boundary — out of scope (EP-07 bounded). Blue checklist verified: engine untouched; no chat/platform import; socket dir 0o700 + socket 0o600. |
| 14 | Update _session_manager/DAEMON_CONTRACT.md if daemon-side singleton/idle-exit behaviour needs documenting | 5 (DOCS) | done | DAEMON_CONTRACT.md updated: corrected the lock override to the --lock flag; added an 'idle-empty auto-exit' lifecycle section distinguishing daemon-level self-exit from the engine's per-session eviction (the WP-003 daemon-side behaviour the WP-002 caller contract did not cover). |
| 15 | Run ruff + mypy on changed files | 6 (LINT) | done | — |
| 16 | Run /code-review against local diff; address findings | 6.5 (REVIEW) | done | /code-review produced bundle at .architecture/change-owned-terminal-shared-session/code-reviews/PR-feat-wp-003-shared-daemon-singleton-2026-06-08T112204Z/ — verdict PASS, 0 findings across all three lenses; REVIEW.md + signals.json present. |
| 17 | Commit (Conventional Commits) + push | 7 (COMMIT) | pending | — |
| 18 | Add in-process unit tests for config resolution (resolve_default_lock / resolve_idle_exit_secs), flock helpers (acquire/release singleton lock), and pty-provider selection seam (_build_pty_adapter / _build_server) in tests/unit/test_daemon_idle_exit.py | 3 (GREEN) | done | discovered during implementation — lifts in-process measured coverage on the helpers the subprocess integration tests cannot instrument; pins config + lock-arbitration behaviour directly |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 2 | 2026-06-08T11:03:03Z | 2026-06-08T11:05:43Z | RED: 5 unit idle-exit tests + 4 integration singleton tests written; fail for the right reason (No module named session_manager_daemon) |
| 3 | 2026-06-08T11:05:50Z | 2026-06-08T11:16:51Z | GREEN: session_manager_daemon.py implemented (IdleEmptyExitWatcher + flock singleton main + injectable pty seam); 21 new tests pass, full related suite 73 pass; 96% measured coverage (subprocess-tracked) on the new file. One self-heal: SIGTERM handler moved before READY (rc -15 -> 0). |
| 4 | 2026-06-08T11:17:00Z | 2026-06-08T11:17:51Z | BLUE: frozen engine untouched; import-graph test confirms no chat/platform/sessionbridge import; socket parent dir 0o700 + socket 0o600 verified on a booted daemon. No extraction needed — the reusable binding primitive is already shared (WP-001); the divergent wiring glue stays per-module (host on retiring path, WP-007). |
| 5 | 2026-06-08T11:17:57Z | 2026-06-08T11:18:55Z | DOCS: DAEMON_CONTRACT.md updated for the WP-003 daemon side — corrected lock override to --lock flag; added idle-empty auto-exit lifecycle section (distinct from per-session eviction; transparent to callers via ensure-daemon restart). |
| 6 | 2026-06-08T11:19:02Z | 2026-06-08T11:20:50Z | LINT: py_compile OK; ruff check + format clean (fixed 1 unused import + 1 format diff); tests green. mypy is not a CI gate; the 2 mypy findings match the pre-existing sanctioned host pattern, no type-ignore noise added (CP). |
| 6.5 | 2026-06-08T11:20:57Z | 2026-06-08T11:23:39Z | addressed: /code-review bundle produced (PR-feat-wp-003-shared-daemon-singleton-2026-06-08T112204Z), verdict PASS, 0 findings (0 inline + 0 remediation + 0 exceptions). Build Verification empty; all 4 files read end-to-end; all 3 lenses produced output. |
| 7 | 2026-06-08T11:24:00Z | (in flight) | — |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|
| 3 | 1 | test_sigterm_cleanly_stops exited -15 (killed by SIGTERM default disposition) instead of clean exit 0 | Signal handlers were installed AFTER READY was printed; a SIGTERM arriving in the window between READY and signal.signal() hit Python's default SIGTERM disposition (terminate, rc=-15), not the clean-stop handler | Install SIGTERM/SIGINT handlers BEFORE binding the socket and printing READY, so any signal arriving the instant READY is observed is caught by the clean-stop handler | re-running |
| 6 | 1 | ruff: F401 unused fcntl import in unit test; ruff format diff in integration test. mypy: 2 findings on the new file (BindingManager->SocketServer arg-type; test-lib session_child_adapters import-not-found). | ruff: leftover import + unformatted block (in-scope, trivial). mypy: NOT a CI gate (CI runs py_compile + ruff + pytest); the 2 findings are IDENTICAL to the pre-existing sanctioned session_manager_host.py pattern (BindingManager.__getattr__ delegation is accepted by the engine by design; the test-lib import is lazy/test-only). | Removed the unused fcntl import; ran ruff format. Did NOT add type-ignore noise the host does not carry (CP: match the established convention). py_compile + ruff + pytest all green. | resolved — CI gate (py_compile + ruff + pytest) green; mypy parity with the existing host module preserved |

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

