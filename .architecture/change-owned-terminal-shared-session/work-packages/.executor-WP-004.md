# Executor journal — WP-004

> Started: 2026-06-08T10:32:00Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| uv (pytest runner) | present | — |
| ruff (lint/format) | present | — |
| gh CLI | present | — |
| mypy (type check) | present | — |

## Plan

**Approach.** EXPAND-Create on the ProviderAdapter port we own (adapter.py). New InteractiveClaudePtyAdapter in _session_manager/adapters/claude_pty.py runs the REAL interactive 'claude --dangerously-skip-permissions --agent sulis' (no -p, no stream-json) in pty io-mode. encode/decode/turn_complete are unused on pty (raise / None / False), mirroring the PtyChildAdapter sibling. Pre-prompt delivery REUSES the launcher's bash-3.2-safe sidecar-cat: import _PRE_PROMPT_SIDECAR (zero duplication), resolve the sidecar by SULIS_CHANGE_ID from the process env (zero SessionSpec change, the contract-note-preferred path), and append the cat positional only when the sidecar file exists. Key risk: confirming the change_id reaches spawn_argv via env not argv — resolved per ADR-004 (daemon sets SULIS_CHANGE_ID on the process env). Real interactive round-trip NOT asserted here (CI cannot run the binary; observed-done deferred to WP-007). Frozen ClaudeAdapter stays untouched; both adapters coexist.

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | Write test_conforms_to_provider_adapter_protocol (isinstance runtime-checkable) in tests/unit/test_claude_pty_adapter.py | 2 (RED) | done | — |
| 2 | Write test_spawn_argv_is_interactive (no -p, no stream-json flags; includes --agent sulis and --dangerously-skip-permissions) in tests/unit/test_claude_pty_adapter.py | 2 (RED) | done | — |
| 3 | Write test_spawn_argv_appends_preprompt_sidecar_cat_when_present (SULIS_CHANGE_ID set + sidecar file exists -> argv carries cat positional) in tests/unit/test_claude_pty_adapter.py | 2 (RED) | done | — |
| 4 | Write test_spawn_argv_omits_positional_when_no_preprompt (no SULIS_CHANGE_ID / no sidecar -> bare interactive argv) in tests/unit/test_claude_pty_adapter.py | 2 (RED) | done | — |
| 5 | Write test_unused_pty_methods (encode raises NotImplementedError; decode returns None; turn_complete returns False) in tests/unit/test_claude_pty_adapter.py | 2 (RED) | done | — |
| 6 | Write test_reuses_launcher_sidecar_constant (adapter references _terminal_launcher._PRE_PROMPT_SIDECAR, not a duplicated literal) in tests/unit/test_claude_pty_adapter.py | 2 (RED) | done | — |
| 7 | Implement InteractiveClaudePtyAdapter in _session_manager/adapters/claude_pty.py per the contract; import _PRE_PROMPT_SIDECAR from the launcher | 3 (GREEN) | done | — |
| 8 | Refactor / boy-scout pass; confirm frozen ClaudeAdapter untouched, both adapters coexist; record Blue note on pre-prompt-delivery choice | 4 (BLUE) | done | Blue note: pre-prompt reaches spawn_argv via the launcher's sidecar file resolved by SULIS_CHANGE_ID env (ADR-004 preferred path) — zero SessionSpec change, frozen spec untouched. Refactor applied: removed needless _shell_quote wrapper, call shlex.quote inline (module-top import). Deferred (own WP, not unbounded side-quest): extract a shared _pre_prompt_sidecar_path(change_id) helper — the path is now built in 2 places (launcher + this adapter) but the launcher builds it inline inside _build_launch_script and extracting it touches the frozen launcher internals across files. |
| 9 | Docs: record pre-prompt-delivery choice in module docstring + WP Blue note (no separate docs file applies) | 5 (DOCS) | done | No docs_to_update frontmatter; no founder-facing or contract markdown enumerates production adapters. Behaviour-change doc lives in the module docstring (EXPAND-Create rationale, pty-no-decode, sidecar reuse, env-sourced change-id + ULID validation, observed-done deferral) + the WP Blue note. The two adjacent docs (manual driver evidence, fixtures stubs README) describe the headless chat path only — out of scope to touch. |
| 10 | Run ruff check + format + mypy | 6 (LINT) | done | ruff check + ruff format --check clean on both new files. py_compile (the CI lint gate) OK. mypy: new claude_pty.py is clean in isolation; the 27 errors are a pre-existing baseline in imported _wpxlib.py + manager.py (frozen claude.py trips the same 11) and mypy is NOT a CI gate (lint = manifest JSON + py_compile; gate = pytest) — out of scope. Full unit suite 2141 passed, 9 skipped, no regressions. |
| 11 | Run /code-review against local diff vs base; address findings | 6.5 (REVIEW) | done | /code-review run; bundle at .architecture/.../code-reviews/PR-feat-wp-004-...-2026-06-08T105205Z/ (REVIEW.md + signals.json). Verdict PASS. 1 critical correctness finding F-01 (spawn_argv emitted a shell $(cat <sidecar>) token, but the manager spawns argv directly with no shell=True per manager.py:626 _spawn_pty_process — it would reach claude as a literal string) found AND fixed inline: adapter now reads the sidecar via read_text and passes the brief text as a single argv element (correct under direct execv; still never shell-parsed). 2 tests updated to match. Re-verified: 11 adapter tests + 39-test adapter/core suite all green; ruff + mypy clean. 0 findings remaining. |
| 12 | Commit (Conventional Commits) + push | 7 (COMMIT) | pending | — |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 1 | 2026-06-08T10:32:58Z | 2026-06-08T10:33:08Z | Worktree feat/wp-004-interactive-claude-pty-adapter cut off change/create-change-owned-terminal-shared-session at b6c65ea7; journal init; preflight uv/ruff/gh/mypy all present |
| 2 | 2026-06-08T10:34:00Z | 2026-06-08T10:35:38Z | 11 RED tests in tests/unit/test_claude_pty_adapter.py; all fail at import (ModuleNotFoundError: claude_pty) — RED for the right reason |
| 3 | 2026-06-08T10:35:51Z | 2026-06-08T10:37:31Z | Implemented InteractiveClaudePtyAdapter in _session_manager/adapters/claude_pty.py; reuses _terminal_launcher._PRE_PROMPT_SIDECAR + validate_change_ulid; all 11 new tests pass; adapter suite 25 pass (frozen ClaudeAdapter untouched). Coverage tool absent (manual analysis: every method+branch exercised, effectively 100% on new file) |
| 4 | 2026-06-08T10:37:38Z | 2026-06-08T10:38:40Z | BLUE: removed needless _shell_quote wrapper (call shlex.quote inline via module-top import); tests stay green (25 pass). Frozen ClaudeAdapter untouched, both adapters coexist. Shared sidecar-path-builder extraction deferred to its own WP (2-consumer but cross-file into the frozen launcher) |
| 5 | 2026-06-08T10:38:46Z | 2026-06-08T10:39:19Z | No-op for external docs: no docs_to_update frontmatter, no founder/contract doc enumerates production adapters. Behaviour fully documented in the new module docstring + WP Blue note |
| 6 | 2026-06-08T10:39:31Z | 2026-06-08T10:47:33Z | ruff check + format clean; py_compile (CI lint gate) OK; new file mypy-clean (pre-existing baseline errors in imported modules are out of scope, mypy not a CI gate); full unit suite 2141 passed / 9 skipped |
| 6.5 | 2026-06-08T10:47:51Z | 2026-06-08T10:54:19Z | addressed: 1 inline fix, 0 remediation, 0 exceptions. /code-review verdict PASS. F-01 (critical correctness: shell $(cat) token inert under the manager's direct-execv pty spawn) fixed inline by reading the sidecar and passing the brief text as a single argv element; 2 tests updated; re-verified green. Bundle: REVIEW.md + signals.json present. |
| 7 | 2026-06-08T10:54:25Z | (in flight) | — |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

