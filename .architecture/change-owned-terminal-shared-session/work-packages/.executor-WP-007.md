# Executor journal — WP-007

> Started: 2026-06-08T12:58:54Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| node | present | — |
| pnpm | present | — |
| python3 | present | — |
| coverage | absent | @vitest/coverage-v8 not installed; manual branch analysis at Step 3 — all functions + branches in ensureDaemon.ts exercised by the 6 unit tests + the composition tests (warm/cold/race/error/import-graph) |

## Plan

**Approach.** Substitute primitive (cockpit ephemeral host -> shared daemon attach). New apps/cockpit/server/lib/ensureDaemon.ts is the Node binding of DAEMON_CONTRACT (sibling of the Python daemon_client.py) — probe stable socket, cold-start detached + READY handshake, idempotent/race-tolerant. index.ts: replace startSessionManagerHost (mkdtempSync ephemeral) with ensureDaemon against ~/.sulis/session-manager.sock; close() detaches sidecar+HTTP but never kills the shared daemon. Key risks: (1) read-only gate allow-list must follow the spawn from index.ts to ensureDaemon.ts; (2) existing serverTerminalLifecycle tests assert host-is-reaped and must be re-pointed to daemon-outlives-cockpit; (3) independence MUST — no chat.ts/SessionBridge/platform coupling.

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | Write ensureDaemon unit tests in apps/cockpit/server/tests/ensureDaemon.test.ts: daemonIsLive probes status round-trip; ensureDaemon warm-path returns live socket without spawn; cold-start spawns daemon detached + waits READY; race-loser polls until live; DaemonStartError on no-READY | 2 (RED) | done | — |
| 2 | Write index migration assertions in apps/cockpit/server/tests/serverSharedDaemon.test.ts: startProductionServer calls ensureDaemon against stable socket (no mkdtempSync temp socket); close() does NOT kill the daemon (only sidecar + HTTP); ensureDaemon returns existing socket when already live (no second spawn) | 2 (RED) | done | — |
| 3 | Update existing tests/serverTerminalLifecycle.test.ts to the shared-daemon wiring (test_graceful_shutdown no longer asserts the daemon is reaped; startSessionManagerHost assertion replaced by ensureDaemon) | 2 (RED) | done | — |
| 4 | Implement apps/cockpit/server/lib/ensureDaemon.ts — Node binding of DAEMON_CONTRACT: daemonIsLive(socketPath) + ensureDaemon({socketPath, python}) with detached spawn, READY handshake, idempotent + race-tolerant poll | 3 (GREEN) | done | — |
| 5 | Re-point apps/cockpit/server/index.ts: replace startSessionManagerHost (mkdtempSync ephemeral) with ensureDaemon against stable socket (SULIS_SESSION_MANAGER_SOCKET default ~/.sulis/session-manager.sock); startProductionServer.close() tears down only sidecar + HTTP, never the daemon; keep socketPath test-injection role | 3 (GREEN) | done | — |
| 6 | Update read-only gate allow-list (tests/read-only-inventory.test.ts): move the sanctioned process-start from index.ts to ensureDaemon.ts (the spawn moves, the gate follows), keep positive assertions | 3 (GREEN) | done | — |
| 7 | Re-point e2e fixed-socket seam: run-terminal-real-server.ts injection now ensures-against the fixed socket (verify it still works with daemon close-semantics) | 3 (GREEN) | done | — |
| 8 | Refactor: extract any shared NDJSON status-probe / framer duplication if 2-consumer threshold fires; otherwise skip | 4 (BLUE) | skipped | No production duplication to extract: ensureDaemon.ts already reuses the shared createNdjsonLineFramer primitive (the EP-03 win). The only candidate duplication is the ~8-line fake-pty-child python helper across the server-tests vs e2e test trees — deliberately NOT extracted: the trees have different module-resolution roots + path depths and the import-boundary rule (import/no-restricted-paths) governs cross-tree imports; the existing codebase already keeps per-tree test substrates separate (terminal-backend.py). The two Node/Python ensure bindings are intentional language siblings of one contract (ADR-001), not extractable. |
| 9 | Update index.ts header docstring + any docs reflecting the ephemeral-host→shared-daemon behaviour change | 5 (DOCS) | done | — |
| 10 | Run typecheck + lint + test gates | 6 (LINT) | done | — |
| 11 | Commit + push | 7 (COMMIT) | in-progress | — |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 2 | 2026-06-08T13:02:15Z | 2026-06-08T13:09:21Z | RED: ensureDaemon.test.ts + serverSharedDaemon.test.ts written; serverTerminalLifecycle.test.ts re-pointed to shared-daemon wiring. All fail for the right reason — module ../lib/ensureDaemon missing. |
| 3 | 2026-06-08T13:09:35Z | 2026-06-08T13:26:02Z | GREEN: ensureDaemon.ts (Node DAEMON_CONTRACT binding) implemented; index.ts re-pointed (ensureDaemon at stable socket, close() never kills the shared daemon, ephemeral host removed); read-only gate (TS + shell halves + check-read-only-script tests) re-pointed to ensureDaemon.ts; e2e run-terminal-real-server re-pointed. Full server suite 705/705 pass. |
| 4 | 2026-06-08T13:26:09Z | 2026-06-08T13:27:02Z | BLUE: production code clean — ensureDaemon.ts reuses the shared createNdjsonLineFramer (EP-03), small focused functions, no copy-paste, independence MUST holds. Conditional cross-test-tree helper extract correctly skipped (import-boundary convention). Tests remain green. |
| 5 | 2026-06-08T13:27:09Z | 2026-06-08T13:29:05Z | DOCS: apps/cockpit/README.md updated (read-only gate seam, live-terminal architecture, daemon-survives-close semantics) + in-code header docs on index.ts/ensureDaemon.ts/read-only gate/e2e wrappers reflect the ephemeral-host→shared-daemon migration. |
| 6 | 2026-06-08T13:29:11Z | 2026-06-08T13:32:54Z | LINT: tsc -p server + tsc -p client clean (one self-heal: block-body arrow for daemon.kill cleanups); npm run lint clean (exit 0). Affected tests re-confirmed green. |
| 6.5 | 2026-06-08T13:33:01Z | 2026-06-08T13:36:38Z | addressed: /code-review run, bundle at .architecture/change-owned-terminal-shared-session/code-reviews/PR-feat-wp-007-cockpit-shared-daemon-2026-06-08T133510Z/ (REVIEW.md + signals.json verified present). Verdict PASS — 0 critical/high/medium; 1 low Watch-List note (materialiseCommand token chain, correct + test-covered, no fix). CR-01 clean, all 3 lenses produced output, 0 anti-patterns. No findings requiring action. |
| 7 | 2026-06-08T13:36:44Z | (in flight) | — |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|
| 6 | 1 | TS2322 x3 in serverSharedDaemon.test.ts: () => daemon.kill('SIGKILL') returns boolean, not void|Promise<void> (the cleanups array type) | ChildProcess.kill returns boolean; arrow expression-body leaks it into the void-typed cleanup callback | wrap daemon.kill in a block-body arrow so it returns void | applying |

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

