# Executor journal — WP-007

> Started: 2026-06-08T00:02:57Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| node | present | — |
| python3 | present | — |
| playwright | present | binary v1.60.0; chromium + npm install run at Step 3 before executing the spec |
| gh CLI | present | — |

## Plan

**Approach.** REINFORCE-Test (the load-bearing end-to-end proof). Drive the SPEC's full 5-item acceptance journey through the cockpit UI against the REAL server endpoint (startProductionServer: spawns the Python host + attaches the sidecar to the /terminal WS) — NOT the e2e harness proxy (MEA-09: real interface, real socket, real pty, no proxy in the round-trip). Reuses the existing isolation harness retargeted at the real server. Two key risks: (1) the client must get NO VITE_TERMINAL_WS_URL so the WP-006 same-origin default is exercised, which means vite must proxy the /terminal WS upgrade to the real Express server; (2) the production host seeds no banner, so the setup drives a real open+feed against the AF_UNIX socket to inject a known scrollback token before the browser attaches.

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | Write test_terminal_renders_scrollback (acceptance #1) in apps/cockpit/e2e/live-terminal-real.spec.ts | 2 (RED) | done | — |
| 2 | Write test_typing_reaches_session (acceptance #2) in apps/cockpit/e2e/live-terminal-real.spec.ts | 2 (RED) | done | — |
| 3 | Write test_reopen_catches_up (acceptance #3) in apps/cockpit/e2e/live-terminal-real.spec.ts | 2 (RED) | done | — |
| 4 | Write test_cross_change_attach_refused (acceptance #4) in apps/cockpit/e2e/live-terminal-real.spec.ts | 2 (RED) | done | — |
| 5 | Write test_read_surfaces_and_chat_unaffected (acceptance #5) in apps/cockpit/e2e/live-terminal-real.spec.ts | 2 (RED) | done | — |
| 6 | Create live-terminal-real-setup.ts: seed change + drive a real open+feed against the real AF_UNIX socket to inject a known scrollback token BEFORE the browser attaches (production host seeds no banner) | 3 (GREEN) | done | — |
| 7 | Create run-terminal-real-server.ts: boot startProductionServer() (spawns host + attaches sidecar) on the isolated seed | 3 (GREEN) | done | — |
| 8 | Create live-terminal-real.config.ts: dedicated isolated ports, client gets NO VITE_TERMINAL_WS_URL (proves WP-006 same-origin default), client vite proxies /terminal WS to the real server, CONFIG.clientOrigin matches client port for the Origin gate | 3 (GREEN) | done | — |
| 9 | Create live-terminal-real-teardown.ts | 3 (GREEN) | done | — |
| 10 | Add a vite WS-proxy config (or proxy entry) so the same-origin /terminal default reaches the real Express sidecar in the e2e | 3 (GREEN) | skipped | 2-consumer threshold not met for a shared NDJSON-rpc abstraction: my spec drives it over a browser WS, my setup over a node Socket+framer, with divergent termination semantics — extracting a cross-transport adapter for 2 divergent sites is premature abstraction against the established per-harness-file convention (terminal-proxy.ts / live-terminal-setup.ts each own their small helper). The shared primitives that DO apply are already reused (seed(), createNdjsonLineFramer). No refactor needed. |
| 11 | Refactor: extract any duplication between the harness-proxy setup and the real-server setup at the 2-consumer threshold | 4 (BLUE) | done | conditional |
| 12 | Add spec header note: this supersedes the harness-proxy e2e as the production proof | 5 (DOCS) | done | — |
| 13 | Add test:e2e:terminal-real npm script + wire it into .github/workflows/cockpit-e2e.yml so branch-ci runs the real-server spec (DoD Blue) | 5 (DOCS) | done | CI gate is named in WP DoD Blue — in-scope for this Test WP |
| 14 | Run lint + type + format | 6 (LINT) | done | — |
| 15 | Code-review gate | 6.5 (REVIEW) | pending | — |
| 16 | Commit + push | 7 (COMMIT) | pending | — |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 2 | 2026-06-08T00:04:00Z | 2026-06-08T00:08:48Z | RED: live-terminal-real.spec.ts written (5 acceptance tests, 1:1 with SPEC). Fails for the right reason — the real-server config/setup harness do not exist yet, so the spec cannot load/run. Truthful selectors verified against ThreadView.tsx + LiveTerminal.tsx (view=, section-terminal, connection status: connected, region 'Live terminal output'). |
| 3 | 2026-06-08T00:09:02Z | 2026-06-08T00:17:18Z | GREEN: 5/5 acceptance tests pass against the REAL server endpoint (twice, stable). New e2e harness: live-terminal-real-setup.ts (seed + pre-seed scrollback via real open+feed over the AF_UNIX socket), run-terminal-real-server.ts (boots startProductionServer → spawned host + attached sidecar), live-terminal-real.config.ts (dedicated ports 5194/5293, NO VITE_TERMINAL_WS_URL — WP-006 same-origin default), vite.real-terminal.config.ts (proxies /terminal WS to the real server), teardown. Minimal in-scope server change: startProductionServer gains a socketPath test-injection option + exposes socketPath on the handle (mirrors existing python option). 669 unit tests green; read-only gate clean (203 files). |
| 4 | 2026-06-08T00:17:25Z | 2026-06-08T00:17:59Z | BLUE: tests remain green. No duplication extraction needed — shared primitives (seed(), createNdjsonLineFramer) already reused; cross-transport NDJSON-rpc abstraction would be premature (2 divergent sites, established per-harness-file convention). DoD Blue invariants confirmed: no developer ~/.sulis read (dedicated seeded state/projects dirs), spec header documents supersession of the harness-proxy e2e, CI runs the new spec. |
| 5 | 2026-06-08T00:18:05Z | 2026-06-08T00:18:35Z | DOCS: live-terminal-real.spec.ts header documents it as the production proof (supersedes the harness-proxy e2e); the harness live-terminal.spec.ts header now points to its successor and notes the proxy/backend may be retired later. npm script test:e2e:terminal-real added; .github/workflows/cockpit-e2e.yml runs it on branch-ci (DoD Blue 'confirm the new spec runs there'). No other docs apply (Test WP, no docs_to_update frontmatter). |
| 6 | 2026-06-08T00:18:40Z | 2026-06-08T00:19:26Z | LINT/TYPE/FORMAT clean: npm run typecheck (tsc server + client) passes; npm run lint (eslint .ts,.tsx across the whole tree incl. e2e) passes with zero findings. Playwright transpiled + ran all 5 specs, confirming e2e TS validity. |
| 6.5 | 2026-06-08T00:19:31Z | 2026-06-08T00:23:19Z | addressed: /code-review ran (high effort), bundle at .architecture/production-terminal-sidecar/code-reviews/PR-feat-wp-007-e2e-real-server-2026-06-08T002130Z/ with REVIEW.md + signals.json + hardening-deltas/INDEX.md + tool-outputs/. Verdict PASS — 0 critical/high/medium; 1 low/awareness-only (per-harness NDJSON rpc helpers intentionally not unified across divergent transports — no delta, CR-04). Build Verification empty (typecheck+lint exit 0); PR Hygiene all clean (0 migrations/schema/secrets, 1 additive CI step). No findings require fixing — proceed to Step 7. |
| 7 | 2026-06-08T00:23:24Z | (in flight) | — |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

