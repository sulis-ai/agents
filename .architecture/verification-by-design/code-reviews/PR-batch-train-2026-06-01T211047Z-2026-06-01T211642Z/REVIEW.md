# Wave 3: plan-work + orchestrator skills + fixtures land

Three WPs shipped: plan-work skill extension (per-WP verification field + slice-end deferred-needs auto-draft); orchestrator skills wiring (specify + draft-architecture + requirements-validation + the template); P-VER fixture suite (8 fail + 4 pass + idempotency).

Mechanical baseline green; per-WP reviews PASS.
Only WP-008 (the dogfood E2E) remains.

Refs CH-01KT2B.
