# Executor journal — WP-007

> Started: 2026-06-08T23:14:42Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| python3 | present | — |
| pytest | present | — |
| ruff | present | — |

## Plan

**Approach.** EXPAND-Extend / additive. Construct a per-session RecoveryDriver at the manager composition root (beside LifecycleManager) and route each kind=error Event from a live session into driver.observe(). The on_event session seam is ALREADY consumed by TurnGuardManager.attach(); the key risk is clobbering it, so the wiring fans on_event out to BOTH the guard observer AND a new _on_error_event sibling hook (the LifecycleManager precedent: a manager method delegating to an injected Armor primitive). STDIN_BROKEN process-death errors are NOT routed (the driver itself already no-ops them, and the lifecycle owns them — no double-handling). Regression guard: the existing session-manager unit+integration suite must stay byte-behaviour-unchanged.

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | Write test_error_event_routes_to_driver in tests/unit/test_session_manager_recovery_wiring.py (live scripted-child session emits an error-kind Event; assert it reaches the driver's observe()) | 2 (RED) | done | — |
| 2 | Write test_process_death_error_not_routed_to_driver in tests/unit/test_session_manager_recovery_wiring.py (STDIN_BROKEN process-death error is NOT routed to the driver — lifecycle owns it) | 2 (RED) | done | — |
| 3 | Write test_recovery_driver_constructed_at_composition_root (manager builds a RecoveryDriver beside LifecycleManager with default policy/clock/classifier/adapter hint) | 2 (RED) | done | — |
| 4 | Write test_non_error_events_not_routed (chunk/result/tool_use events do not reach the driver) | 2 (RED) | done | — |
| 5 | Construct the RecoveryDriver at the composition root in _session_manager/manager.py (one wiring line beside LifecycleManager) + add the _on_error_event sibling hook fanning out from on_event without clobbering the guard | 3 (GREEN) | done | — |
| 6 | Refactor: extract the on_event fan-out cleanly if 2-consumer threshold fires (guard + recovery observers) | 4 (BLUE) | skipped | 2-consumer threshold not reached within file scope. The Event-error-construction shape in _log_append echoes lifecycle.py/_surface_mid_turn_error and session.py/_append_stdin_broken_error, but those are different modules — extracting across three modules is a cross-cutting change outside this WP's single-file (manager.py) scope (EP-07). BLUE applied: typed the driver registry as dict[str, RecoveryDriver] (no new mypy error) + removed a stray docstring artifact in the _send comment. |
| 7 | Update _session_manager/__init__.py module docstring to note WP-007 wires the driver (docs reflect behaviour change) | 5 (DOCS) | done | — |
| 8 | Run ruff + mypy (no NEW type errors beyond the 12 pre-existing in manager.py) | 6 (LINT) | done | — |
| 9 | Run /code-review against the local diff | 6.5 (REVIEW) | done | — |
| 10 | Commit + push feat/wp-007-manager-wiring | 7 (COMMIT) | done | — |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 2 | 2026-06-08T23:16:34Z | 2026-06-08T23:18:17Z | RED: 5 wiring tests written (driver construction, error routing, STDIN_BROKEN-not-routed, non-error-not-routed, guard-coexistence); all fail for the right reason — no recovery_driver_factory wiring + no _on_error_event hook. |
| 3 | 2026-06-08T23:18:26Z | 2026-06-08T23:40:58Z | GREEN: per-session RecoveryDriver constructed at composition root (default factory builds real driver) + _on_error_event sibling hook fans out from on_event alongside the guard (additive, guard untouched). Surfaced+fixed a real deadlock: driver.observe re-enters the FIFO via send/submit, so routing dispatches on an isolated daemon thread (pump never blocks on itself). STDIN_BROKEN filtered (no double-handling). 13 wiring tests pass; all new manager.py lines covered; regression guard 72 passed byte-unchanged. |
| 4 | 2026-06-08T23:41:04Z | 2026-06-08T23:42:31Z | BLUE: typed _recovery_drivers as dict[str, RecoveryDriver] + _make_recovery_driver -> RecoveryDriver (eliminated the one new mypy error my wiring introduced — back to the 12 pre-existing tuning-dict errors, zero added). Removed a stray docstring artifact. 2-consumer extraction did not fire in-file. 13 wiring tests green; diff is manager.py-only. |
| 5 | 2026-06-08T23:42:37Z | 2026-06-08T23:43:11Z | DOCS: updated _session_manager/__init__.py module docstring — the 'unwired to the live manager until WP-007' narrative replaced with the live wiring (per-session driver at composition root, _on_error_event sibling hook chaining additively onto on_event, off-thread routing, STDIN_BROKEN filter, additive guarantee). Package imports clean. |
| 6 | 2026-06-08T23:43:15Z | 2026-06-08T23:44:22Z | LINT: ruff check clean (all 3 files); ruff format applied (manager.py + test file reformatted to single-line factory assignment). mypy manager.py = 11 errors, ALL pre-existing tuning-dict/object pattern (lines 139-203, 400) — ZERO in my new code (>=510), zero added by the wiring. 13 wiring tests green post-format. |
| 6.5 | 2026-06-08T23:44:27Z | 2026-06-08T23:47:56Z | addressed: /code-review run; bundle at .architecture/automation-reliability-recovery/code-reviews/PR-feat-wp-007-manager-wiring-2026-06-08T234452Z/ (REVIEW.md + signals.json + tool-outputs). Verdict PASS. Build Verification empty (0 PR-introduced errors; mypy 11==11 base/head). 1 low finding (per-error daemon thread, manager.py:650) — Watch-List, bounded by one-in-flight invariant, no failing test (CR-04), no action needed. 0 deltas. Proceeding to Step 7. |
| 7 | 2026-06-08T23:48:02Z | 2026-06-08T23:50:02Z | Pushed to feat/wp-007-manager-wiring at SHA 3b8554c9e68911f6eaf83853740578ed16ed3a32 (3 files, 888 insertions). Push accepted; local==remote tip. Conventional Commit; pre-commit hook passed (no --no-verify). Commit unstamped (no run-ulid in session env — origin degrades to inferred, non-fatal per ADR-013). Calling session can proceed with Step 8+ (this is a Python plugin marketplace — no deploy; the test suite is the gate). |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

