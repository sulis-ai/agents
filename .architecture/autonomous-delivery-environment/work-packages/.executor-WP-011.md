# Executor journal — WP-011

> Started: 2026-06-04T15:43:04Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| node+npm | present | — |
| git+python3 (sulis-change/clone) | present | — |

## Plan

**Approach.** EXPAND-Create full round-trip (Journey H+J). New lib/discovery/startFromIntent.ts (deterministic classify + orchestrator), a StartChangeRunner port + SulisChangeStarter adapter that execFiles 'sulis-change start' + 'git clone' SERVER-SIDE (the WP-010 lesson: never delegate the act to the bridge agent), a confirm-gated SSE route registered in the one sanctioned write file (chat.ts), and the UI (intent box + concierge route-offer). Reuses WP-010 confirmGate + the SpineEmitterMinter execFile/resolveScriptsDir pattern + WP-009 detectRoute. Key risk: read-only gate must allow-list the new process-start adapter without a new write-verb file (ADR-006); investigation must be CONTAINED in a real change, never inline.

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | Write startFromIntent.classify.test.ts — intent→{primitive,slug} via deterministic classifier (FR-29); ambiguous intent ⇒ INTENT_AMBIGUOUS (one clarifying question, no guess); investigation kind resolves to a change not inline (FR-34/N9) | 2 (RED) | done | — |
| 2 | Write startChangeRunner.contract.test.ts — the StartChangeRunner port: a Fake + the real SulisChangeStarter both satisfy clone()/start() contract; real adapter starts a REAL change against temp SULIS_STATE_DIR + temp repo and lands at recon (FR-29/30) | 2 (RED) | done | — |
| 3 | Write startFromIntent.clone.test.ts — absent repo ⇒ clone from fixture-local-repo then start at Recon (FR-30); broken clone ⇒ REPO_UNREACHABLE + NO change started (all-or-nothing) | 2 (RED) | done | — |
| 4 | Write routes.startChange.test.ts — supertest: propose→started SSE; confirm-gated; 422 INTENT_AMBIGUOUS/START_CONFIRM_STALE; 409 SESSION_BUSY; investigation containment (real change created, no inline exploration); one act-log line, never intent text | 2 (RED) | done | — |
| 5 | Extend read-only-inventory.test.ts — SulisChangeStarter is the new sanctioned process-start site; start-from-intent adds NO new write-verb file (stays {chat.ts}); the lib + orchestrator start no process | 2 (RED) | done | — |
| 6 | Write StartFromIntent.test.tsx — intent box / concierge route-offer → proposal (primitive+slug+repo plan) → confirm → change-appears at Recon; investigation framed as a contained change; ambiguity surfaced; tokens.css only | 2 (RED) | done | — |
| 7 | Implement lib/discovery/startFromIntent.ts — pure classify(intent,kind) + StartFromIntentOrchestrator (confirm gate reuse, propose/confirm, clone-then-start, investigation containment, all-or-nothing); starts no process | 3 (GREEN) | done | — |
| 8 | Implement ports/StartChangeRunner.ts + adapters/SulisChangeStarter.ts — execFile sulis-change start (--repo-root/--primitive/--slug/--intent/--base) + git clone; resolveSulisChangeScript dir; SULIS_STATE_DIR honored; deterministic server-side (the WP-010 lesson) | 3 (GREEN) | done | — |
| 9 | Implement routes/startChange.ts createStartChangeRouter in chat.ts (the one sanctioned write-verb file) — SSE, 422/409/502 typed errors, act-log; wire into app.ts mount /api/changes/start-from-intent | 3 (GREEN) | done | — |
| 10 | Implement client: api/useStartFromIntent.ts + streamStartFromIntent funnel in client.ts + components/StartFromIntent.tsx + styles + pages/StartFromIntentPage.tsx + /start route in App.tsx | 3 (GREEN) | done | — |
| 11 | Refactor — extract shared SSE/act-log/classifier duplication at 2-consumer threshold (e.g. resolveSulisChangeScript, slugify) | 4 (BLUE) | done | 2-consumer threshold fired: extracted resolvePluginScriptsDir (shared by SulisChangeStarter + SpineEmitterMinter); extended the shared writeSseFrame union to include start-from-intent events |
| 12 | Docs — update contracts/DATA-CONTRACT-GUIDE.md / cockpit README if start-from-intent behaviour needs documenting | 5 (DOCS) | done | cockpit README HTTP-surface table + header row; founder-readable DATA-CONTRACT-GUIDE start + investigation rows |
| 13 | Run typecheck + lint + check:read-only | 6 (LINT) | done | — |
| 14 | Run /code-review against the diff and address findings | 6.5 (REVIEW) | done | code-review bundle PASS; 1 medium quality finding (render-phase onStarted side-effect) fixed inline via useEffect; re-verified green |
| 15 | Commit + push | 7 (COMMIT) | pending | — |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 2 | 2026-06-04T15:44:55Z | 2026-06-04T15:49:33Z | RED: classify + port-contract/real + clone + route + read-only-gate + component tests written; all fail for the right reason (modules not yet implemented) |
| 3 | 2026-06-04T15:49:38Z | 2026-06-04T16:00:10Z | GREEN: lib/discovery/startFromIntent.ts (classify+orchestrator), StartChangeRunner port + SulisChangeStarter adapter (real sulis-change start + git clone, server-side deterministic), createStartChangeRouter in chat.ts, resolveProjectRepo, client useStartFromIntent + streamStartFromIntent + StartFromIntent.tsx + /start route. Full suite green: 102 files / 733 tests. New code covered incl. a REAL change created at recon against temp SULIS_STATE_DIR. |
| 4 | 2026-06-04T16:00:16Z | 2026-06-04T16:03:21Z | BLUE: extracted resolvePluginScriptsDir shared primitive (SulisChangeStarter + SpineEmitterMinter, 2-consumer); extended shared writeSseFrame union. Typecheck + 737 tests + read-only gate all green. |
| 5 | 2026-06-04T16:03:28Z | 2026-06-04T16:04:28Z | DOCS: added start-from-intent to the cockpit README HTTP-surface table + audited-write-path header; extended the founder-readable DATA-CONTRACT-GUIDE with start + investigation-containment rows (Journeys H+J). |
| 6 | 2026-06-04T16:04:33Z | 2026-06-04T16:04:57Z | LINT: eslint clean; tsc (server+client) clean; read-only gate clean (151 files, no mutating ops). |
| 6.5 | 2026-06-04T16:05:03Z | 2026-06-04T16:08:24Z | addressed: /code-review bundle produced (REVIEW.md + signals.json) at .architecture/autonomous-delivery-environment/code-reviews/PR-feat-wp-011-start-from-intent-2026-06-04T160641Z; verdict PASS; 1 medium quality finding (render-phase onStarted side-effect) resolved inline (moved to useEffect); 0 findings remain; full suite + typecheck + lint + read-only gate green. |
| 7 | 2026-06-04T16:08:31Z | (in flight) | — |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

