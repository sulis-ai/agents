# Executor journal — WP-005

> Started: 2026-06-04T11:04:46Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| node+npm | present | — |
| vitest | present | — |
| tsc+eslint | present | — |
| live claude (for live round-trip) | absent | RecordedSessionBridge fixture covers send/stream/resume/spawn/mismatch/incomplete in CI; live hop is BLOCK-and-hand-to-founder |

## Plan

**Approach.** EXPAND-Create (an adapter for OUR SessionBridge port; the claude CLI is called by the adapter, never wrapped — ADR-002). Full-round-trip slice built as ONE branch in the load-bearing RGB order: port+contract → safety libs (binding/lock) → recorded fixture → relay route + read-only-gate extension → prod adapter → composer/SSE UI. Key risk: this is the app's first write/act path, so the binding guard (fail-closed, zero-bytes on mismatch) and the read-only gate extension (exactly one app.post + one process-start) are the highest-leverage assertions. The recorded-bridge fixture makes send→stream→resume→spawn→mismatch→incomplete-step CI-testable WITHOUT a live claude; the live reply is the BLOCK-and-hand-to-founder human hop.

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | Write session-bridge.contract.test.ts — the reusable runContract suite (resolve returns live\ | resumable\ | done | 2 (RED) |
| 2 | Write sessionBinding.test.ts — A→A bound; A→B mismatch (zero bytes); cwd-mismatch ⇒ mismatch; same verdict live/resumed/spawned | 2 (RED) | done | — |
| 3 | Write inFlightLock.test.ts — acquire/held/release; double-acquire ⇒ busy; release after complete frees | 2 (RED) | done | — |
| 4 | Write session-bridge.recorded.test.ts — runContract vs RecordedSessionBridge over all four fixture cases + FR-N5 mid-step resumed assertion | 2 (RED) | done | — |
| 5 | Write session-bridge.streamjson.test.ts — runContract vs StreamJsonSessionBridge with a stubbed stream-json child (CI parity) | 2 (RED) | done | — |
| 6 | Write routes.chat.test.ts — supertest: SESSION_BUSY 409, SESSION_CHANGE_MISMATCH 422 zero bytes, SESSION_UNREACHABLE 502 not-delivered, state→chunk→complete, mid-stream drop→interrupted+partial, structured-log-no-body | 2 (RED) | done | — |
| 7 | Extend read-only-inventory.test.ts + check-read-only-script.test.ts — exactly one process-start (bridge), one app.post (relay); reads start none; new process-start rule | 2 (RED) | done | — |
| 8 | Write Composer.test.tsx — FR-23 states, FR-20 disable, FR-22 partial+interrupted, FR-19 not-delivered, FR-26 honest resumed note, chips/slash/Enter-Shift | 2 (RED) | done | — |
| 9 | Write useChatStream.test.tsx — SSE state→chunk*→complete; error events map to plain-English state | 2 (RED) | done | — |
| 10 | Implement ports/SessionBridge.ts (resolveSession + relay + SessionResolution/RelayOutcome/sink types) | 3 (GREEN) | done | — |
| 11 | Implement lib/sessionBinding.ts (pure fail-closed change_id + cwd equality) and lib/inFlightLock.ts (per-change in-memory lock) | 3 (GREEN) | done | — |
| 12 | Implement adapters/RecordedSessionBridge.ts + the recording-bridge-claude-session fixture (live/resume/spawn/mid-step recorded stream-json) | 3 (GREEN) | done | — |
| 13 | Implement adapters/StreamJsonSessionBridge.ts (headless claude -p stream-json; resume/spawn; never synthesise completion; the one process-start site) | 3 (GREEN) | done | — |
| 14 | Implement routes/chat.ts (lock→resolve→bind→act→stream SSE→release; typed error codes; one structured log line) and mount in app.ts; wire adapter selection in index.ts | 3 (GREEN) | done | — |
| 15 | Extend check-read-only.sh — allow-list relay app.post + bridge process-start; add process-start-elsewhere rule | 3 (GREEN) | done | — |
| 16 | Implement client api/useChatStream.ts (SSE reader through a single funnel) + components/Composer.tsx (chips+slash+Enter/Shift, streaming render, pause/stop run-controls, neutral user bubble) matching the signed contract; dock into ThreadView | 3 (GREEN) | done | — |
| 17 | Refactor: extract shared SSE-event parsing / duplication if 2-consumer threshold fires across recorded+streamjson adapters | 4 (BLUE) | done | 2-consumer threshold DID fire (recorded + prod adapters both parse stream-json): extracted lib/streamJsonToEvents.ts (parse + map + leadingStateFor + resumedFor) at authoring time; both adapters consume it, so a parser bug shows identically in both — the parity guarantee. No further 2-consumer duplication found (writeSse is route-only; describeError single-use). |
| 18 | Docs: update apps/cockpit/README.md chat-relay section if docs apply | 5 (DOCS) | done | README applied: read-only framing now 'read-only except the one audited relay seam'; chat relay added to the endpoints table + a dedicated WP-005 section; error-code list extended (SESSION_BUSY/MISMATCH/UNREACHABLE); gate description updated with the new process-start rule + allow-list; structure tree updated with the new port/adapters/route |
| 19 | Run typecheck + lint + format + check-read-only.sh | 6 (LINT) | done | typecheck clean; eslint clean; WP-005 files prettier-formatted; check-read-only.sh clean (111 files); npm run dev boots and the relay route matches live (the parametric-mount bug was caught + fixed via the live-boot self-verify) |
| 20 | Run /code-review against the local diff and address findings | 6.5 (REVIEW) | done | /code-review run; bundle at .architecture/.../code-reviews/PR-feat-wp-005-...-2026-06-04T113728Z/ (REVIEW.md + signals.json). Verdict PASS — 0 findings across architecture/security/quality lenses; 0 hardening deltas. Build verification empty; 10 source files covered by 9 tests + the contract suite. |
| 21 | Commit + push | 7 (COMMIT) | done | Committed b86711a (Conventional Commits, no --no-verify); pushed to origin/feat/wp-005-two-way-chat-roundtrip (new branch, tracking set). Local + remote SHA match. |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 1 | 2026-06-04T11:05:20Z | 2026-06-04T11:05:25Z | Worktree at ../wp-005-worktree on feat/wp-005-two-way-chat-roundtrip off change/create-autonomous-delivery-environment (dev SHA 957b492); deps installed; baseline tests green |
| 2 | 2026-06-04T11:06:20Z | 2026-06-04T11:12:16Z | 9 RED test files written (contract suite, binding, lock, recorded+streamjson adapters, chat route supertest, read-only-gate extensions, Composer, useChatStream); all fail for the right reason (missing modules/adapters/fixture/components) |
| 3 | 2026-06-04T11:12:22Z | 2026-06-04T11:25:37Z | Full round-trip implemented: SessionBridge port + binding/lock libs + shared stream-json mapper + RecordedSessionBridge (4-case fixture) + StreamJsonSessionBridge (prod, one process-start site) + relay route (lock→resolve→bind→act→SSE→release) + read-only gate extension + client streamChat funnel + useChatStream hook + Composer docked in ThreadView. Whole suite 485/485 green; gate clean. resolveSession unit test added for prod resolver. |
| 4 | 2026-06-04T11:25:42Z | 2026-06-04T11:25:57Z | Shared stream-json mapper (lib/streamJsonToEvents.ts) extracted at the 2-consumer threshold (recorded + prod adapters) — the parity guarantee. No further duplication at threshold. Suite stays 485/485 green after refactor. |
| 5 | 2026-06-04T11:26:03Z | 2026-06-04T11:27:39Z | apps/cockpit/README.md updated to reflect the behaviour change: the cockpit is now read-only EXCEPT the one sanctioned chat relay (POST /api/changes/:id/chat, SSE); endpoints table + dedicated relay section + extended error codes + the new process-start gate rule + structure tree all reflect WP-005 |
| 6 | 2026-06-04T11:27:44Z | 2026-06-04T11:36:12Z | typecheck + eslint + check-read-only.sh all clean; full suite 485/485; npm run dev boots and the live relay route matches POST correctly (fixed an Express-5 parametric-mount bug the in-process tests didn't surface) |
| 6.5 | 2026-06-04T11:36:17Z | 2026-06-04T11:39:08Z | addressed: /code-review PASS, 0 findings (0 inline + 0 remediation + 0 exceptions). Bundle at .architecture/.../code-reviews/PR-feat-wp-005-...-2026-06-04T113728Z/ with REVIEW.md + signals.json. Architecture/security/quality lenses all clean; build verification empty; new source fully test-covered. |
| 7 | 2026-06-04T11:39:14Z | 2026-06-04T11:53:14Z | Fix-forward: pushed to feat/wp-005-two-way-chat-roundtrip at SHA ed652c3220f33581fbc02342568e8c94a32aaecc (b86711a..ed652c3). Two live-round-trip bugs fixed on the same branch: (1) dedicated CONFIG.chatBridgeStartupTimeoutMs (60s) replaces gitTimeoutMs(5s) for bridge startup; (2) spawn stdio ['ignore','pipe','pipe'] closes child stdin (kills the 3s stall). New chat-bridge-startup.test.ts pins both. Full suite 490/490; typecheck/lint/read-only gate clean; npm run dev boots (server 5174, client 5173). Calling session can re-drive the live round-trip + proceed with Step 8+. |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|
| 3 | 1 | routes.chat: unreachable returned SSE-200 not 502; SESSION_BUSY test timed out | SSE opened eagerly so zero-byte failures couldn't return a JSON status; supertest first-request was lazy so the lock wasn't held before the 2nd send | Lazy-open SSE on first emit (zero-byte failure ⇒ clean 502/422 JSON, FR-19/N3); dispatch the busy-test's first request via .then() so it fires before the 2nd | routes.chat 10/10 green |
| 3 | 2 | read-only gate over-fired: new process-start rule flagged existing read/recreate spawns (SulisChangeStoreReader/Recreator); inventory .delete( pattern matched Set.delete in inFlightLock | the new ADR-003 process-start rule was broader than the sanctioned set; the MVP HTTP-verb pattern was an unscoped bare .delete( | Allow-listed the existing audited subprocess sites in both gate script + inventory test; scoped HTTP-verb patterns to (app | router). prefix |
| 6 | 3 | Live npm-run-dev server returned 405 on POST /api/changes/:id/chat though supertest returned 200 | Express 5 / path-to-regexp v8 mis-matches a POST against a PARAMETRIC app.use mount ('/api/changes/:id/chat') over a real HTTP socket; supertest's in-process inject tolerated it. (A stale background server compounded the diagnosis.) | Mounted the chat router at the LITERAL '/api/changes' prefix (same shape as the GET changes router) and matched '/:id/chat' inside the router; moved express.json() into the router. Verified live: empty prompt→400, valid+unknown-change reaches the change store (404 with the real helper) | Live server matches POST /api/changes/:id/chat correctly; route + handler wired |
| 7 | 1 | Live round-trip over a real socket: every live chat failed SESSION_UNREACHABLE. Bug 1 — index.ts wired startupTimeoutMs: CONFIG.gitTimeoutMs (~5s); a cold headless claude takes ~5-9s to first output (measured: first output 4924ms, full reply 9221ms), so the watchdog fired right as the agent woke. | Bridge startup-to-first-output budget reused the git-operation budget (5s); a cold model start legitimately exceeds it. | Added dedicated CONFIG.chatBridgeStartupTimeoutMs (default 60000ms, override CHAT_BRIDGE_STARTUP_TIMEOUT_MS, parsed positive-int) in server/config.ts; index.ts now wires startupTimeoutMs: CONFIG.chatBridgeStartupTimeoutMs (NOT gitTimeoutMs). Regression: chat-bridge-startup.test.ts asserts the dedicated value >=45s, distinct from gitTimeoutMs, and source-guards the index.ts wiring. | Fixed; new tests green; full suite 490/490. |
| 7 | 2 | Bug 2 — child stdin left open => 3s stall. Real claude logs 'Warning: no stdin data received in 3s, proceeding without it'. The prompt is passed via -p so the child needs no stdin; the open stdin added ~3s dead time before any output, compounding bug 1's watchdog fire. | spawnClaudeBridge did not guarantee the child's stdin was at EOF; an open-but-idle stdin makes claude wait 3s before proceeding. | server/adapters/StreamJsonSessionBridge.ts: spawn stdio is now ['ignore','pipe','pipe'] (stdin ignored => immediate EOF; stderr piped for observable diagnostics) with an explaining comment. Regression: chat-bridge-startup.test.ts source-guards stdio[0]=='ignore' AND a behavioural test runs a real fake claude on PATH that times its first stdin read — asserts <500ms (a closed stdin hits EOF in ~single-digit ms; an open-idle stdin blocks ~2s, reproducing the stall). Verified the behavioural test FAILS (blocked 2349ms) when stdin is flipped back to 'pipe'. | Fixed; behavioural + source guards green; full suite 490/490; typecheck/lint/read-only-gate clean; npm run dev boots (server 5174 HTTP 200, client 5173 HTTP 200). |

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

