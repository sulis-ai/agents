# Executor journal — WP-010

> Started: 2026-06-04T15:05:19Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|

## Plan

**Approach.** Fix-forward. The confirm->mint phase mints nothing because handleConfirm delegates the mint to the bridge agent (ADR-007), which is slow/fragile and produced no entities live. Make the mint DETERMINISTIC + SERVER-SIDE: a new SpineMinter port + SpineEmitterMinter adapter invoke the spine-emitter CLIs (sulis-emit-tenant/-product) + a schema-validated Project write into the active SULIS_STATE_DIR .brain/instances, all-or-nothing + idempotent; git init is also a deterministic server action. The bridge stays for search/ask conversation only. Key risk: the new process-start site must be added to the read-only gate allow-list; the orchestrator itself stays process-free so the emitter-only static assertion holds.

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | RED: routes.onboarding.mint-real.test.ts — drive confirm against a TEMP SULIS_STATE_DIR; assert real Tenant+Product+Project jsonld land in .brain/instances with Project.source persisted (FR-36) | 2 (RED) | done | — |
| 2 | RED: all-or-nothing — a failing emit leaves NO partial graph (FR-N11); idempotency — re-confirm does not duplicate (FR-31) | 2 (RED) | done | — |
| 3 | GREEN: new ports/SpineMinter.ts port (mint + git-init), so the orchestrator stays process-free | 3 (GREEN) | done | — |
| 4 | GREEN: new adapters/SpineEmitterMinter.ts — resolves emitter script dir like resolveHelperPath; execFile sulis-emit-tenant/-product + validated Project save; all-or-nothing staging; git init for create branch | 3 (GREEN) | done | — |
| 5 | GREEN: onboardingOrchestrator handleConfirm calls deps.spineMinter (deterministic) instead of relay(buildMintPrompt); bridge kept for search/ask only | 3 (GREEN) | done | — |
| 6 | GREEN: wire SpineMinter through chat.ts onboarding route + app.ts + index.ts (real adapter); tests inject a fake | 3 (GREEN) | done | — |
| 7 | GREEN: update check-read-only.sh rule 2b allow-list to add SpineEmitterMinter.ts as a sanctioned process-start site; document it | 3 (GREEN) | done | — |
| 8 | GREEN: fix discovery.emitter-only.test.ts behavioural assertion (mint no longer relays a mint prompt — assert deterministic minter invoked instead); keep orchestrator no-spawn static assertion | 3 (GREEN) | done | — |
| 9 | BLUE: extract emitter-script-dir resolution shared with index.ts resolveHelperPath if 2-consumer threshold fires | 4 (BLUE) | skipped | 2-consumer threshold not met: resolveEmitterScriptsDir (emitter dir, plugin-cache fallback) and index.ts resolveHelperPath (sulis-list-changes, in-repo only) resolve different targets with different fallback chains — not the same primitive. No extraction warranted. |
| 10 | DOCS: amend ADR-007 + WP-010 note — mint is server-side-deterministic (emitter CLIs invoked by the server); bridge keeps the conversation only | 5 (DOCS) | done | — |
| 11 | Run typecheck + lint + read-only gate + full suite + npm run dev boots | 6 (LINT) | done | — |
| 12 | Commit + push | 7 (COMMIT) | pending | — |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 2 | 2026-06-04T15:06:21Z | 2026-06-04T15:07:16Z | RED: discovery.mint-real.test.ts drives the real adapter against a temp brain — fails (module SpineEmitterMinter absent); asserts real Tenant+Product+Project land, source persisted, all-or-nothing, idempotent, git init |
| 3 | 2026-06-04T15:07:29Z | 2026-06-04T15:28:19Z | GREEN: new SpineMinter port + SpineEmitterMinter adapter (resolves emitter scripts via plugin-cache, execFile sulis-emit-tenant/-product + validated emit-project.py + git init; all-or-nothing staging; idempotent). Orchestrator handleConfirm now uses deterministic minter not bridge-mint. Wired through chat.ts route + app.ts + read-only gate allow-list (both bash + vitest). Full suite 700/700 green; mint-real 70/70 isolated runs stable. |
| 4 | 2026-06-04T15:28:33Z | 2026-06-04T15:28:47Z | BLUE: reviewed adapter for duplication; conditional script-dir extraction skipped (different resolution targets, threshold not met). Tests green after review. |
| 5 | 2026-06-04T15:28:52Z | 2026-06-04T15:29:39Z | DOCS: amended ADR-007 (mint is server-side deterministic; agent keeps conversation only; real-mint CI verification added) + WP-010 fix-forward note. |
| 6 | 2026-06-04T15:29:45Z | 2026-06-04T15:31:04Z | LINT: typecheck clean, eslint clean, read-only gate clean (143 files), full suite 700/700 green, npm run dev boots (cockpit server up on 127.0.0.1:5174). |
| 6.5 | 2026-06-04T15:31:09Z | 2026-06-04T15:33:46Z | addressed: /code-review run, bundle at .architecture/autonomous-delivery-environment/code-reviews/PR-feat-wp-010-onboarding-roundtrip-2026-06-04T153137Z. Verdict PASS — 0 critical/high/medium; 1 low/awareness Watch-List note (SSE error message may echo founder's own path to their own UI — not a leak, no delta per CR-04). Build Verification empty; 700/700 tests green. |
| 7 | 2026-06-04T15:33:52Z | (in flight) | — |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

