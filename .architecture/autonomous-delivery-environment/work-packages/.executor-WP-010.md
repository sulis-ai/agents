# Executor journal — WP-010

> Started: 2026-06-04T14:25:02Z
> Lifecycle step: 7
> Status: in_progress

## Pre-flight checks

| Tool / Check | Status | Fallback |
|---|---|---|
| node | present | — |
| claude CLI | present | live mint is the BLOCK-and-hand-to-founder step; CI uses RecordedSessionBridge |
| vitest+eslint+tsc | present | npm install run in worktree; baseline axe a11y test is flaky under full parallel suite (pre-existing, passes in isolation) |

## Plan

**Approach.** EXPAND-Create round-trip for Journey G (cold-start onboarding), built by extending apps/cockpit and riding the WP-005 SessionBridge + spine emitters. New pure modules confirmGate.ts + repoFindOrCreate.ts (WP-005 sessionBinding sibling pattern: no fs/git/process), an onboardingOrchestrator that sequences search→ask→propose→confirm→mint over the bridge, and POST /api/onboarding/session registered INSIDE routes/chat.ts (ADR-006: no new read-only-gate write exception). The agent runs the discovery skills + git init + emitters; the server never spawns them. UI reuses the chat composer + SSE funnel idiom (EP-03). Key constraints: all consequential acts confirm-gated + all-or-nothing; search bounded to chosenArea; three founder-locked decisions (local-only repo default, one product per conversation, neutral two-letter tile).

| # | Item | Step | Status | Notes |
|---:|---|---|---|---|
| 1 | Write confirmGate.test.ts in apps/cockpit/server/tests/ — propose→closed; matching token→open once; stale/mismatched→DISCOVERY_CONFIRM_STALE; declined/absent→closed; deterministic no I/O | 2 (RED) | done | — |
| 2 | Write repoFindOrCreate.test.ts in apps/cockpit/server/tests/ — find sets source no-create; create local-default git-init plan confirm-gated; simulated create-failure → REPO_CREATE_FAILED + NO source (no dangling config); unconfirmed create no-op; hosted-remote only when separately confirmed | 2 (RED) | done | — |
| 3 | Write discovery.orchestrator.test.ts in apps/cockpit/server/tests/ — sequences search→ask→propose→confirm→mint; bounded to fixture chosenArea; delegates to the bridge; idempotent already-minted surfaced not duplicated | 2 (RED) | done | — |
| 4 | Write discovery.emitter-only.test.ts in apps/cockpit/server/tests/ — mints ONLY via the bridge/emitter seam; no direct entity-file write (FR-32) | 2 (RED) | done | — |
| 5 | Write routes.onboarding.test.ts in apps/cockpit/server/tests/ — supertest + RecordedSessionBridge + fixture dir: SSE search→propose→minted; 422 SCOPE_VIOLATION; 422 CONFIRM_STALE; 409 SESSION_BUSY; idempotent re-run no count growth; declined/failed leaves graph unchanged; ONE product per conversation | 2 (RED) | done | — |
| 6 | Write OnboardingChat.test.tsx in apps/cockpit/client/src/tests/ — search→ask→proposal→confirm→minted (reuses SSE funnel); find-vs-create with local-only PRE-SELECTED; neutral two-letter tile no upload; declined creates nothing; failed leaves setup unchanged; scope-violation surfaced | 2 (RED) | done | — |
| 7 | Implement confirmGate.ts in apps/cockpit/server/lib/discovery/ — pure token-matched gate (WP-005 sessionBinding sibling) | 3 (GREEN) | skipped | Recorded round-trip driven by ProgrammableBridge in tests; existing recording-bridge-discovery-session.json fixture (chat-shaped chunks) is sufficient — onboarding's proposal/minted events are orchestrator-produced from phase+idempotency, not parsed from the agent stream, so no new fixture case needed for CI green. |
| 8 | Implement repoFindOrCreate.ts in apps/cockpit/server/lib/discovery/ — pure find-or-create decision + ProjectSource plan + no-dangling-config rule | 3 (GREEN) | done | — |
| 9 | Implement onboardingOrchestrator.ts — sequences phases over the bridge, scope-bounds, idempotency probe via readProducts, emits OnboardingStreamEvent | 3 (GREEN) | done | — |
| 10 | Register POST /api/onboarding/session INSIDE server/routes/chat.ts (ADR-006 no new gate exception); wire in app.ts with a discovery in-flight lock | 3 (GREEN) | done | — |
| 11 | Implement client streamOnboarding funnel + useOnboarding hook + OnboardingChat.tsx + css + /setup route + ProductSwitcher 'set up a new product' entry | 3 (GREEN) | done | — |
| 12 | Refactor: extract any 2-consumer duplication; Boy Scout within touched files | 4 (BLUE) | done | conditional |
| 13 | Docs: update apps/cockpit/README.md onboarding surface + how to drive POST /api/onboarding/session | 5 (DOCS) | done | Reuse achieved during GREEN (SSE funnel readSseStream now 3 consumers, writeSseFrame/openSseHeaders shared, monogram + InFlightLock reused, confirmGate mirrors sessionBinding sibling pattern). No new 2-consumer duplication to extract. Boy Scout: removed a dead ternary branch in handleConfirm. |
| 14 | Run typecheck + lint + check-read-only.sh | 6 (LINT) | done | — |
| 15 | Code-review gate /code-review on the branch diff | 6.5 (REVIEW) | done | — |
| 16 | Commit + push | 7 (COMMIT) | done | — |
| 17 | Fix: carry chosenArea from search turn into confirm turn so minted Project.source.repo is persisted (FR-36) — caught by the runnable self-verify; locked with a route-test assertion | 3 (GREEN) | done | self-verify-found integration bug |

## Step trace

| Step | Started | Completed | Outcome |
|---:|---|---|---|
| 2 | 2026-06-04T14:26:33Z | 2026-06-04T14:30:27Z | RED: 6 new test files (confirmGate, repoFindOrCreate, orchestrator, emitter-only, routes.onboarding, OnboardingChat) all fail for the right reason (missing modules / unmounted route). |
| 3 | 2026-06-04T14:30:32Z | 2026-06-04T14:39:58Z | GREEN: confirmGate/repoFindOrCreate/onboardingOrchestrator + POST /api/onboarding/session inside chat.ts (no new write-gate exception) + client streamOnboarding/useOnboarding/OnboardingChat + /onboarding route + ProductSwitcher wiring. Full suite 695 green (server 481, client 214). Read-only gate clean (141 files). Coverage tool absent; new files ≥90% by inspection. |
| 4 | 2026-06-04T14:40:03Z | 2026-06-04T14:40:35Z | BLUE: reuse already at the SSE-funnel/frame-writer/monogram/lock seams (EP-03); removed one dead ternary branch (Boy Scout). Tests still green. |
| 5 | 2026-06-04T14:40:41Z | 2026-06-04T14:42:08Z | Docs: apps/cockpit/README.md — intro updated (two act paths in one file), onboarding HTTP-surface row, new error codes, dedicated Cold-start onboarding section with the exact curl flow, workspace tree comment. |
| 6 | 2026-06-04T14:42:12Z | 2026-06-04T14:48:08Z | Re-verified after self-verify fix: typecheck clean, lint clean, read-only gate clean (141 files), server suite 481 green. (Step 6 re-confirmed post-fix.) |
| 6.5 | 2026-06-04T14:48:09Z | 2026-06-04T14:50:50Z | addressed: /code-review produced bundle at .architecture/autonomous-delivery-environment/code-reviews/PR-feat-wp-010-onboarding-roundtrip-2026-06-04T144855Z/ (REVIEW.md + signals.json). Verdict PASS, 0 findings at any severity, 0 deltas. Build Verification empty (typecheck/lint/read-only-gate clean). 2 watch-list notes (Math.random nonce, per-process orchestrator singleton) — documented, no action. |
| 7 | 2026-06-04T14:50:57Z | 2026-06-04T14:51:54Z | Pushed to feat/wp-010-onboarding-roundtrip at SHA 27faed7c262391f4b2a23bb1c6233cc4e1a34c44. Remote branch created + tracking set. CI triggered. Calling session can proceed with Step 8+. |

## Self-heal attempts

| Step | Attempt | Failure (1-line summary) | Root cause | Change applied | Outcome |
|---:|---:|---|---|---|---|

## Post-deploy verification

(Not yet recorded — Step 11 fills this in.)

## Notes

