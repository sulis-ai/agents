{
  "name": "sulis-ai-agents",
  "owner": {
    "name": "Iain"
  },
  "metadata": {
    "version": "1.13.0",
    "description": "Sulis AI plugin marketplace — methodology studios, requirements analysis, facilitation, context cartography, right-sized architecture, disciplined change primitives, deterministic code intelligence, founder coordination, and autonomous Work Package execution end-to-end. v1.13.0 ships sulis-execution v0.9.0 — a three-layer architectural pivot that fixes the structural parking failure of v0.5.1 through v0.8.3. Layer 1: a deterministic CLI tool library (plugins/sulis-execution/scripts/wpx-*) wraps every cross-cutting bookkeeping operation in stdlib-only Python — journal management, INDEX status flips with atomic --expected checks, WP frontmatter reads, BLOCKER writes per EL-08, findings register with signature-hash dedup, auto-draft WP creation, git worktree management, and the full Steps 8-10 pipeline (CI poll + rebase + squash-merge + deploy + health + smoke). Layer 2: the executor agent narrows its contract from Steps 1-12 to Steps 1-7 (worktree, RGB, docs, lint, commit, push) and delegates ALL bookkeeping to wpx-* tools via Bash — direct Markdown edits to journal / INDEX / BLOCKER / findings files are FORBIDDEN. Layer 3: the calling session (run-all / run-wp skill) owns Steps 8-12 — invokes wpx-pipeline via top-level Bash(run_in_background:true) for the long CI + deploy + health waits (the harness's cross-turn notification works reliably for top-level agents but NOT subagents, which is why prior versions parked); then spawns sulis-security:security-reviewer for Step 11; then wpx-step12 wrap for atomic acceptance evidence + INDEX flip + worktree remove. All v0.8.x capabilities preserved (pre-flight tooling check, coverage fallback, migration-lock serialisation, parallel batch dispatch, findings register + auto-draft WPs, human-cancellation pattern). Composes with git-workflow-standard.md (GIT-01..GIT-11), executor-loop-standard.md (EL-01..EL-08), red-green-blue.md (RGB-01..03), CP-01..CP-05, AAF-01..AAF-09, EP-02/03/07."
  },
  "plugins": [
    {
      "name": "srd",
      "source": "./plugins/srd",
      "version": "1.18.1",
      "description": "Requirements Analyst — facilitates building Software Requirements Documents backed by UML artifacts through guided conversation. Includes intent triage (Phase 1), disambiguation sweep (Phase 3.5), and adversarial sweep (Phase 3.6 — produces MISUSE_CASES.md with negative requirements). v1.18.1 ships git-workflow-standard.md v0.1.3 — two clarifications from WP-7 first-live-run gaps: GIT-04 new MUST subsection 'CI must run on feature branches' (with example trigger config covering Conventional Commits prefix globs) + new SHOULD subsection 'Local pre-commit fallback' for transitional projects (with explicit guard-rails: journal record, recommendation surfaced to founder); GIT-05 new anti-pattern subsection 'git switch dev from a worktree' — explicit canonical merge paths (host API preferred for all cases, refspec push acceptable shortcut for single-commit branches only, multi-commit must use host API). Composes with executor-loop-standard.md (EL-01..EL-08). v1.18.0 prior: GIT-04 concurrency + GIT-05 rebase + GIT-10 rollback-during-parallel + NEW GIT-11 hot-fix path."
    },
    {
      "name": "sulis-context",
      "source": "./plugins/sulis-context",
      "version": "0.3.1",
      "description": "Context Cartographer — discovers existing architecture documentation, ADRs, conventions, and standards in a project; produces .context/{project}/INDEX.md so downstream plugins (SRD, SEA, sulis-security) respect prior decisions instead of restating them. Folder-structure-agnostic; works on any project shape."
    },
    {
      "name": "sulis-strategy",
      "source": "./plugins/sulis-strategy",
      "version": "1.3.1",
      "description": "Business strategy studio — vision, strategy, principles, commercial, GTM, roadmap, and domain-specific research"
    },
    {
      "name": "sulis-product-development",
      "source": "./plugins/sulis-product-development",
      "version": "1.2.1",
      "description": "Product development studio — design, plan, implement, complete, feature lifecycle, validation, and autonomous sub-agents"
    },
    {
      "name": "sulis-builder",
      "source": "./plugins/sulis-builder",
      "version": "1.2.1",
      "description": "Studio builder — create new domain expertise packages (7-file studio bundles) using the studio-creation sequence"
    },
    {
      "name": "sulis-design",
      "source": "./plugins/sulis-design",
      "version": "1.3.1",
      "description": "Design studio — design language, tokens, visual identity, customer experience, and cross-outcome coherence verification"
    },
    {
      "name": "sulis-platform-sdk",
      "source": "./plugins/sulis-platform-sdk",
      "version": "0.1.0",
      "description": "Platform SDK — build production-ready SaaS backends with built-in auth, billing, multi-tenancy, and observability"
    },
    {
      "name": "sea",
      "source": "./plugins/sea",
      "version": "0.11.5",
      "description": "Senior Engineering Architect — designs hardened architectures, audits brownfield code for primitive gaps, and decomposes designs into atomic Work Packages with Red-Green-Blue verification. /sea:probe v0.9.0 runs 17 deterministic phases via ast-grep + lizard + scc + git plus optional tools (jscpd, ts-prune, vulture, deadcode, dependency-cruiser, import-linter, detect-secrets). Polyglot 4-stage workspace enumeration discovers TS, Python, Go, Rust, Terraform, and deployment-only dirs alongside monorepo manifests. Phase 1.16 (Deployment Topology) catalogues Dockerfiles, K8s manifests, Helm charts, Terraform, GitHub Actions, Argo CD, Flux, and Sulis manifests (Workload/Application/BusinessManifest) with secret-reference extraction (NAMES only). Phase 1.17 (Credential Scanning) invokes detect-secrets with a strict hash-only privacy contract — plaintext never appears in any output — and respects .secrets.baseline for new-vs-known triage. Outputs structured JSON + Markdown + self-contained navigable HTML. Tools auto-install via the bundled installer; no LLM fallback. Every WP carries a change primitive (22-primitive Minto-pyramid catalogue). Enforces No-Band-Aid-Wrappers (MUST). Reads .context/{project}/INDEX.md (sulis-context); computes SIZING.md (sFPC + ASR). Consumes SRD v1.11.0+ MISUSE_CASES.md, GLOSSARY.md, ## Deferred to SEA journal sections, and HANDOFF_TO_SEA.md."
    },
    {
      "name": "sulis-security",
      "source": "./plugins/sulis-security",
      "version": "0.3.1",
      "description": "Security & Viability Reviewer — runs a 25-primitive codebase viability assessment (security, data protection, code quality, supply chain, infrastructure) via an OODA-spiral methodology. Composes with SEA for hardening and SRD for requirements-traceable findings."
    },
    {
      "name": "idc",
      "source": "./plugins/idc",
      "version": "0.3.1",
      "description": "Investor Deck Coach — facilitates Sequoia-style pitch deck creation through guided one-question-at-a-time conversation. Stage-aware (angel, pre-seed, seed, Series A, Series B). Produces evidence-backed market research with tiered sources, stage-appropriate financial modelling (Excel + branded HTML dashboard), adversarial investor-objection review, customer-branded PowerPoint + HTML decks (Reveal.js), and a live rehearsal drill."
    },
    {
      "name": "sulis-execution",
      "source": "./plugins/sulis-execution",
      "version": "0.9.0",
      "description": "Work Package Executor — three-layer architecture. Layer 1: deterministic CLI tool library at plugins/sulis-execution/scripts/wpx-* (journal, index, wp, blocker, findings, worktree, pipeline, step12) wraps every cross-cutting bookkeeping operation in stdlib-only Python. Layer 2: executor agent owns Steps 1-7 (worktree, RGB, docs, lint, commit, push) and delegates bookkeeping to wpx-* tools via Bash — direct Markdown edits to journal / INDEX / BLOCKER files are FORBIDDEN. Layer 3: calling session (run-all / run-wp skill) owns Steps 8-12 — invokes wpx-pipeline run via top-level Bash(run_in_background:true) for CI poll + rebase + squash-merge + deploy + health + smoke (15-45 min wall time, harness-notification mechanism that's reliable for top-level agents); then spawns sulis-security:security-reviewer Agent for Step 11; then wpx-findings register + auto-draft-wp for non-CRITICAL findings; then wpx-step12 wrap for atomic acceptance evidence + INDEX flip + worktree remove. Fixes the structural parking failure of v0.5.1 through v0.8.3 — every prior version moved the parking boundary because the executor (a subagent) was asked to poll across turns, and the cross-turn notification contract doesn't reliably deliver to a terminated subagent session. v0.9.0 removes polling from the executor entirely. All v0.8.x capabilities preserved (pre-flight tooling check, coverage fallback, migration-lock serialisation, parallel batch dispatch, findings register + auto-draft WPs). Composes with git-workflow-standard v0.1.3, CP-01..CP-05, AAF-01..AAF-09, EP-02/03/07, RGB-01..03."
    },
    {
      "name": "sulis-concierge",
      "source": "./plugins/sulis-concierge",
      "version": "0.1.5",
      "description": "Founder Concierge — the **VP of Engineering** for non-technical founders building a product through the Sulis AI marketplace. Owns the 7-phase journey (Greet → Discover → Specify → Design → Implement → Verify → Secure), directs specialist agents (SRD, SEA, sulis-context, sulis-execution, sulis-security), reads their produced artifacts, translates output into plain English. v0.1.5 updates Phase 5 to invoke the `/sulis-execution:run-all` skill (which runs the dispatch loop inline) rather than spawning a separate orchestrator subagent — fixes the agent-tree-depth issue observed when subagents try to spawn further subagents. v0.1.4 added Phase 5 slice-end auto-draft WP review (companion to sulis-execution findings-register flow): concierge reads each finding's plain-English summary, presents in AAF-06 three-list shape, walks founder through disposition (schedule / skip / merge into existing). v0.1.3 spawn pattern; v0.1.2 Decision Discipline; v0.1.1 Brevity Discipline."
    }
  ]
}
