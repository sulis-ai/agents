// WP-001 — placeholder client root.
//
// Renders a single anchor string so the skeleton.test.tsx assertion has
// something to match. WP-011 replaces this body with the real Router +
// Shell + TanStack Query wiring.

export function App() {
  return (
    <main>
      <p>cockpit booting…</p>
    </main>
  );
}
