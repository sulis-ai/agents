change_id: "01KTR56K1ZRDJ8SD0KFRME81PT"
handle: "CH-RDJ8SD"
slug: "stale-change-binding"
primitive: "fix"
intent: "Fix: a newly-spawned change session inherits a STALE SULIS_CHANGE_ID, binding the new window to the WRONG change (#107). Systematic from the cockpit: the cockpit's background service runs with a SULIS_CHANGE_ID in its env, and every change spawned from it inherits the service's pointer instead of its own — the new window's cwd+branch are correct (the new change's worktree) but SULIS_CHANGE_ID points at a previous change. Observed: chat-ui worktree 01KTR44PX88BGMM2M2HM79N5KY (branch change/feat-chat-ui-within-summary-user) but env SULIS_CHANGE_ID=01KTR381 (a different change). _terminal_launcher.py launch.sh DOES export the correct id (~line 324) but the inherited value shadows it in claude's interactive shell; no stale value in shell profiles (confirmed). TWO-LAYER FIX: (1) LAUNCHER — make the new id authoritative in claude's process env so an inherited parent/cockpit value can't win: unset SULIS_CHANGE_ID before export AND/OR invoke I'm ready to help. What would you like to work on?

I can see you're on the `change/fix-change-reopen-preprompt` branch with some uncommitted changes (mostly `.brain/instances/` lifecycle run files and a modified `roadmap.jsonld`). Let me know what you'd like to do — whether that's continuing work on the change-reopen preprompt fix, reviewing the current diff, or something else entirely. so it's set on claude's process directly. Test: launch.sh run with a pre-set stale SULIS_CHANGE_ID yields the NEW id. (2) AGENT BODY (robust defense) — change-context binding TRUSTS THE WORKTREE over the env when they disagree: when SULIS_CHANGE_ID resolves to a change != the worktree's own .changes manifest change, auto-resolve to the worktree's change, correct the env, log it, proceed with a one-line note (the body's current 'resolves to null' section covers null but not the wrong-change case). Files: plugins/sulis/scripts/_terminal_launcher.py (_build_launch_script ~283-340, the claude invocation), plugins/sulis/agents/sulis.md (change-context section), + the cockpit spawn path if it sets/forwards env. BUILD CAUTION: integrate via direct-merge-on-green per wave (verify each branch CI, squash-merge to the change branch, run the full unit suite), not the train — the WP-branch-collision fix (#105) isn't shipped yet so this change's own WPs still hit that collision."
branch: "change/fix-stale-change-binding"
worktree_path: "/Users/iain/.sulis/changes/01KTR56K1ZRDJ8SD0KFRME81PT/worktree"
base_branch: "main"
base_sha: "2a448be96f7fe4ff729810b4e09190932eda1824"
started_at: "2026-06-10T06:58:30Z"
