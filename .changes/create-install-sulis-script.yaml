change_id: "01KSNX2W1REMRTKCCV570K7S3J"
handle: "CH-01KSNX"
slug: "install-sulis-script"
primitive: "create"
intent: "Create plugins/sulis/scripts/install-sulis.sh — top-level installer covering everything the sulis plugin needs across all skills. Core (git, gh, python3 ≥3.11), analyse-codebase deps (delegates to existing install-probe-tools.sh: ast-grep, lizard, scc, detect-secrets), code-health tier scanners (hadolint, trivy, gitleaks, semgrep, jscpd, ts-prune/vulture/deadcode, optional ruff/mypy/eslint/clippy/golangci-lint). Supports --check (audit only), --core-only, --with-code-health, --all. macOS via brew + pipx + cargo + npm; Linux via apt/dnf + pipx + cargo + npm. Graceful degrade (each missing tool is a warning, not an error). Plus a small co-shipped cleanup: fix the two pre-existing marketplace.json drifts — metadata.version 1.98.1 → 1.111.0 and plugins[sulis].version 0.55.1 → 0.67.0 so the registry matches reality."
branch: "change/create-install-sulis-script"
worktree_path: "/Users/iain/Documents/repos/agents-change-create-install-sulis-script"
base_branch: "dev"
base_sha: "686a1e528d312b59ddea8dbcac034b0526ba24e5"
started_at: "2026-05-27T23:42:28Z"
