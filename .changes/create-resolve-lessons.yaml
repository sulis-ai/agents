change_id: "01KSPZ1MGBXCSFWKZJ6VDBT3MM"
handle: "CH-01KSPZ"
slug: "resolve-lessons"
primitive: "create"
intent: "Build /sulis:resolve-lessons — an orchestrator that proactively drains the lesson backlog. Reads open lesson-labelled GitHub issues, drafts a change per actionable one (spec from the issue body, like the auto-draft-WP mechanism #43), collision-aware schedules them (group by file-touch via recon code-area pointers; serialize same-file, parallelize disjoint), dispatches a worker (subagent/executor) per change through the existing ship pipeline INCLUDING the step-4.5 review gate, closes the issue on merge via the Closes-#N trailer, batch-reports. Optional design-debate team (agent-teams) for SEA-design-heavy lessons. Three non-negotiable guardrails: review gate stays, founder disposition (approve which lessons auto-resolve/skip/defer), collision-aware scheduling never naive-parallel. First run supervised. Reuses: capture-lessons backlog, sulis-issues, run-all loop pattern, change-as-primitive, recon code-area pointers. First real batch will be #53 + #52 (they overlap on the git-workflow standard — tests the collision scheduler)."
branch: "change/create-resolve-lessons"
worktree_path: "/Users/iain/Documents/repos/agents-change-create-resolve-lessons"
base_branch: "dev"
base_sha: "15d5774a407dddf3b75b92a7fab188e78cd775b7"
started_at: "2026-05-28T09:35:59Z"
