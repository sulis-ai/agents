# DEPRECATED

This directory is a deprecation shim. The concierge agent + skills moved to
the canonical `sulis` plugin at v0.2.0 (2026-05-23).

See `README.md` for the migration table and
`../sulis/CHANGELOG.md` v0.2.0 for the rationale.

When working anywhere founder-facing, the concierge lives at
`plugins/sulis/agents/concierge.md`; invoke via `claude --agent sulis`.
