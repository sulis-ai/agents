{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sulis.co/discover-project/tool/read-package-json/input/1.0.0",
  "title": "read-package-json — input",
  "description": "Input to the read-package-json Tool — invoked by the read-package-manifests Step.",
  "type": "object",
  "required": ["path"],
  "additionalProperties": false,
  "properties": {
    "path": {
      "type": "string",
      "description": "Absolute or repo-relative path to a candidate package.json file. The Tool returns a typed null payload if the file is absent rather than erroring (missing manifest is a legitimate state per the Step's postcondition).",
      "minLength": 1
    }
  }
}
