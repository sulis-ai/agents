{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sulis.co/dna/schema/decision/1.0.0",
  "title": "Decision (ADR)",
  "description": "a recorded, non-trivial choice with alternatives + consequences",
  "type": "object",
  "unevaluatedProperties": false,
  "required": [
    "id",
    "title",
    "state",
    "decision",
    "options_considered",
    "sys_status"
  ],
  "properties": {
    "sys_status": {
      "enum": [
        "active",
        "archived",
        "deleted",
        "purged"
      ]
    },
    "valid_from": {
      "type": "string",
      "format": "date-time",
      "description": "business-truth window start \u2014 when this fact became true in the world; null = since-forever"
    },
    "valid_to": {
      "type": "string",
      "format": "date-time",
      "description": "business-truth window end \u2014 when this fact stopped being true; null = still valid"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "reliability score for this instance; 1.0 = certain, 0 = no confidence"
    },
    "produced_by_change": {
      "type": "string",
      "pattern": "^dna:change:[0-9A-HJKMNP-TV-Z]{26}$",
      "description": "the Change that created this entity (prov:wasGeneratedBy); reverse-query gives a change's creation set (#67)"
    },
    "evolved_by_change": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^dna:change:[0-9A-HJKMNP-TV-Z]{26}$"
      },
      "description": "the Changes that revised this entity, in order (prov:wasRevisionOf); reverse-query gives a change's revision set (#67)"
    },
    "id": {
      "type": "string",
      "pattern": "^dna:decision:[0-9A-HJKMNP-TV-Z]{26}$"
    },
    "kind": {
      "enum": [
        "adr",
        "bdr"
      ],
      "default": "adr",
      "description": "decision-kind discriminator — 'adr' (technical architecture decision) or 'bdr' (business decision); additive-optional, absent reads as 'adr' (§9.1)"
    },
    "title": {
      "type": "string"
    },
    "state": {
      "enum": [
        "proposed",
        "accepted",
        "superseded"
      ]
    },
    "context": {
      "type": "string"
    },
    "decision": {
      "type": "string"
    },
    "options_considered": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1
    },
    "consequences": {
      "type": "string"
    },
    "supersedes": {
      "type": "string",
      "pattern": "^dna:(decision):[0-9A-HJKMNP-TV-Z]{26}$"
    }
  },
  "x-schema-org-extends": "schema:Action"
}