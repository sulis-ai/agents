{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sulis.co/dna/schema/experiment/1.0.0",
  "title": "Experiment",
  "description": "a test of a hypothesis against a metric (build-measure-learn)",
  "type": "object",
  "unevaluatedProperties": false,
  "required": [
    "id",
    "hypothesis",
    "success_metric",
    "result",
    "sys_status"
  ],
  "properties": {
    "sys_status": {
      "enum": [
        "active",
        "archived",
        "deleted",
        "purged"
      ]
    },
    "valid_from": {
      "type": "string",
      "format": "date-time",
      "description": "business-truth window start \u2014 when this fact became true in the world; null = since-forever"
    },
    "valid_to": {
      "type": "string",
      "format": "date-time",
      "description": "business-truth window end \u2014 when this fact stopped being true; null = still valid"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "reliability score for this instance; 1.0 = certain, 0 = no confidence"
    },
    "produced_by_change": {
      "type": "string",
      "pattern": "^dna:change:[0-9A-HJKMNP-TV-Z]{26}$",
      "description": "the Change that created this entity (prov:wasGeneratedBy); reverse-query gives a change's creation set (#67)"
    },
    "evolved_by_change": {
      "type": "array",
      "items": {
        "type": "string",
        "pattern": "^dna:change:[0-9A-HJKMNP-TV-Z]{26}$"
      },
      "description": "the Changes that revised this entity, in order (prov:wasRevisionOf); reverse-query gives a change's revision set (#67)"
    },
    "id": {
      "type": "string",
      "pattern": "^dna:experiment:[0-9A-HJKMNP-TV-Z]{26}$"
    },
    "hypothesis": {
      "type": "string"
    },
    "tests": {
      "type": "string",
      "pattern": "^dna:(opportunity|requirement):[0-9A-HJKMNP-TV-Z]{26}$"
    },
    "success_metric": {
      "type": "string",
      "pattern": "^dna:(metric):[0-9A-HJKMNP-TV-Z]{26}$"
    },
    "result": {
      "enum": [
        "supported",
        "refuted",
        "inconclusive"
      ]
    },
    "ran": {
      "type": "string",
      "format": "date-time"
    }
  },
  "x-schema-org-extends": "custom"
}