#!/usr/bin/env python3
"""sulis-emit-opportunity — emit Opportunity entity from an SRD's Summary."""

from __future__ import annotations

import argparse
import json
import subprocess
import sys
from pathlib import Path

_HERE = Path(__file__).resolve().parent
if str(_HERE) not in sys.path:
    sys.path.insert(0, str(_HERE))

from _entity_adapter_local import LocalFileEntityAdapter  # noqa: E402
from _entity_repository import EntityValidationError  # noqa: E402
from _opportunity_emission import emit_opportunity_from_srd  # noqa: E402


def _resolve_repo_root(explicit: str | None) -> Path:
    if explicit:
        return Path(explicit).resolve()
    try:
        out = subprocess.run(
            ["git", "rev-parse", "--show-toplevel"],
            capture_output=True, text=True, check=True, timeout=10,
        )
        return Path(out.stdout.strip())
    except (subprocess.CalledProcessError, FileNotFoundError, subprocess.TimeoutExpired):
        return Path.cwd()


def _ok(data: dict) -> int:
    print(json.dumps({"ok": True, "data": data}, indent=2))
    return 0


def _err(message: str) -> int:
    print(json.dumps({"ok": False, "error": message}, indent=2))
    return 1


def main() -> int:
    parser = argparse.ArgumentParser(description="Emit Opportunity entity from an SRD.")
    parser.add_argument("--from-srd", required=True, help="path to the SRD markdown")
    parser.add_argument("--base-dir")
    parser.add_argument("--domain", default="product-development")
    parser.add_argument("--repo-root")
    args = parser.parse_args()

    srd_path = Path(args.from_srd).resolve()
    if not srd_path.exists():
        return _err(f"SRD not found: {srd_path}")
    repo_root = _resolve_repo_root(args.repo_root)
    base_dir = Path(args.base_dir).resolve() if args.base_dir else repo_root / ".brain" / "instances"

    adapter = LocalFileEntityAdapter(base_dir=base_dir, domain=args.domain)
    try:
        opps = emit_opportunity_from_srd(srd_path, adapter)
    except EntityValidationError as exc:
        return _err(str(exc))

    entries = [
        {"id": o["id"], "path": str(base_dir / args.domain / "opportunity" / f"{o['id'].rsplit(':', 1)[-1]}.jsonld")}
        for o in opps
    ]
    return _ok({"from_srd": str(srd_path), "count": len(entries), "entities": entries})


if __name__ == "__main__":
    sys.exit(main())
