# Fixture: repo-clean

Placeholder. The fixture clone where `origin/main` IS an ancestor of `origin/dev` (no drift) lands here in WP-009 when the full eight-fixture suite for `drift_check.sh` is authored. See `.architecture/auto-back-merge-on-release/work-packages/WP-009-drift-check-test-suite.md`.

Expected `drift_check.sh` behaviour against this fixture: exit 0, silent.
