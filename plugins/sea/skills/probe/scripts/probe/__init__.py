"""
/sea:probe — Python orchestrator for deterministic code intelligence.

Replaces the bash-inline tool invocations of v0.7.x. All flags, exclusions,
and tool-version pins live in `config.py`. All institutional memory from
v0.7.0 → v0.7.2 bug fixes is centralised there.

Public entry point: `probe.py` at the parent directory.
"""

__version__ = "0.8.0"
